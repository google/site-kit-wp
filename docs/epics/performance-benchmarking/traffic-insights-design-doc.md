# **\[SK\] Part 3 \- Traffic Insights Design**

| Reviewer | Role | Status | Last Change |
| :---- | :---- | :---- | :---- |
| [Mariya Moeva](mailto:mmoeva@google.com) | Approver | Not Started |  |
| [Evan Mattson](mailto:emattson@google.com) | Approver | Not Started |  |

***Visibility:** Confidential*   
***Status:*** *Review*  
***Author(s):** [Eugene Manuilov](mailto:eugene.manuilov@fueled.com)*  
***PRD:** [Performance intelligence: site benchmarking & forecasts in Site Kit \[PRD\]](https://docs.google.com/document/d/1zwM9ogRlrFO__rLFVYT6SE1qqsjIwzfFUELBiLnYHUQ/edit?pli=1&tab=t.0)*  
***Figma Designs:** [Figma](https://www.figma.com/design/MWN8TXAjfTeKLF0DZ91bIX/Performance-benchmarking?node-id=552-10409&m=dev)*  
***Last Major Revision:** Aug 18, 2026* 

# **Context**

## **Objective**

This epic makes the `analyticsTrafficOverview` widget interpret the site's traffic rather than only report it. It adds a generated insight to both of the widget's tabs, an expected-baseline range and a 28-day forecast to the Typical Traffic chart, and markers on the Traffic Overview chart for recently published content that is gaining traffic — and it builds the plugin-side infrastructure needed to reach the Site Kit Service's generative endpoints.

## **Background**

The widget currently reports numbers well and says nothing about them. A reader gets their visitor total with a change badge, thirteen months of their own daily traffic, and a ranked account of which channels, devices, referrers, queries and posts moved — and is left to decide for themselves whether any of it is good.

That last step is the one most site owners cannot take. A 12% dip is a crisis or a season depending on context the dashboard has never supplied, and the ranked factors say *what* changed without saying whether the change was expected. Two capabilities close that gap.

The first is an expectation to compare against: a range, derived from the site's own history, saying what a normal day looks like for this site at this time of year — and the same model run forward, so that the next four weeks have a shape rather than being blank. That is arithmetic over a daily series the plugin already reads.

The second is a natural-language reading of the comparison, in the site owner's own language, which is the job of the generative endpoint the Site Kit Service has built. **The generative component is new to Site Kit.** No part of the plugin currently calls a generative endpoint, and `Google_Proxy` has no method for one. This epic builds that path.

# **Design**

## **Overview**

We will add six things to the widget, behind a new `trafficInsights` feature flag:

1. **A generated insight on both tabs.** A compact teaser at the top of Traffic Overview ([Figma](https://www.figma.com/design/MWN8TXAjfTeKLF0DZ91bIX/Performance-benchmarking?node-id=740-40731&m=dev)), and the full three-part insight at the top of Typical Traffic ([Figma](https://www.figma.com/design/MWN8TXAjfTeKLF0DZ91bIX/Performance-benchmarking?node-id=552-10450&m=dev)). One generation serves both.  
2. **An expected-baseline band on the Typical Traffic chart**, drawn across the selected date range inside the thirteen-month window, from a model computed in PHP over the site's own daily history.  
3. **A 28-day forecast** continuing past the end of the period as a dashed line under a band that widens with distance, with a one-line projection beneath the chart.  
4. **Content markers on the Traffic Overview chart**: a dot on the traffic line for each day something was published, with a tooltip naming the posts.  
5. **Insight-driven ordering of the key factors**, so the section that explains the most goes first according to the model rather than according to arithmetic alone.  
6. **A feedback prompt beneath the full insight on Typical Traffic** ([Figma](https://www.figma.com/design/MWN8TXAjfTeKLF0DZ91bIX/Performance-benchmarking?node-id=552-10477&m=dev)), asking whether the narration above it was helpful.

Behind them, six new pieces:

1. **Proxy access to the generative endpoint and to its operations.** `Google_Proxy` gains a method that posts to the service's `/v1/ai/benchmarking` endpoint in asynchronous mode and a second that reads a long-running operation back from `/v1/ai/operations/:operation_id`.  
2. **Two datapoints that carry the generative call and nothing else.** `POST:benchmarking-insight` takes an encoded payload in a request body and submits it, answering with either an operation ID or the finished insight. `GET:benchmarking-insight` takes an operation ID and reads that operation back. Neither runs a report, and the pair is the whole of how the plugin waits — see [Waiting for the generation](#waiting-for-the-generation).  
3. **The expected-baseline model.** PHP that derives the expected daily range the Typical Traffic chart plots, runs the same model 28 days forward for the look-ahead, and produces the period-scale summary the generative request carries — see [Expected baseline](#expected-baseline).  
4. **The request payload, assembled in PHP.** `GET:benchmarking-data` gains one field: the whole generative request, encoded as an opaque string that crosses the browser byte for byte and is handed back to `POST:benchmarking-insight` unread.  
5. **The baseline and the markers on the existing response.** The same datapoint gains the per-day bounds, the forecast rows, the look-ahead object and the content markers, carried in the [wire format](#wire-format-additions) it already uses.  
6. **A datastore slice that waits.** The `benchmarking` slice gains two fetch stores, a resolver that submits the payload once and polls the operation it gets back until the insight arrives, and its own browser cache entry for the result.

**Nothing about this feature is cached on the server.** The data response is cached by the API layer under the selected date range, the insight is cached by the slice itself under the date range and locale, and the service holds its own copy of the generation for 24 hours — see [Browser-side caching](#browser-side-caching).

**The browser derives nothing, and forwards the payload without reading it.** Every figure, ranking, row cap and bound is computed in PHP; the payload is assembled and encoded in the same request and travels back out through `POST:benchmarking-insight` byte for byte. The widget decodes only what it draws. The service narrates; it does not query, and it does not model.

**The numbers and the narration are two independent requests, and that is the point.** Both charts, the totals, the factors and the baseline render as soon as the data response resolves, and the insight fills in behind them however long generation takes. No PHP request holds a report call and a model call together, so neither `max_execution_time` nor a proxy in front of the site can cost the widget its data. **A failed narration must never take the numbers down with it**, and after this epic that is structural rather than careful.

**The whole widget, insight included, works in the view-only dashboard.** All three datapoints are shareable, so the reports and the generative call run under the module owner's credentials on a shared request, exactly as every other shared report does — see [Dashboard sharing](#dashboard-sharing).

![][image1]

## **Infrastructure**

The front end is additive to a widget that already exists: two more sections, two more chart columns and a set of overlays, in a card whose tabs, states and data flow are in place. The server-side gathering and the datapoint that does it are in place too; this epic extends both rather than introducing them.

Eleven reuses carry weight in the design, because it depends on a particular property of each:

* **`Google_Proxy::request()`** already injects `site_id`/`site_secret` and the bearer header, which keeps the submit method thin. **It is `wp_remote_post()` only, and it puts the credentials in the request body**, so the operation read cannot use it as it stands — see [Google\_Proxy additions](#google_proxy-additions).  
* **`Module::set_data()` dispatches through the same `execute_data_request()` path `get_data()` does**, so `get_oauth_client_for_datapoint()` resolves the owner's client for a write on a shared request exactly as it does for a read. That is what carries the submit into the view-only dashboard.  
* **`Site_Goals_Settings_Datapoint`** is the shape a `GET`/`POST` datapoint pair over a shared abstract base already takes on this module, and `POST:save-site-goals-settings` is already a shareable write datapoint declaring the dashboard-viewing capability.  
* **The `EDITABLE` branch of the module data route defaults to `MANAGE_OPTIONS` where the `READABLE` branch defaults to the view-insights capability.** `resolve_datapoint_permission()` resolves `"{$method}:{$datapoint}"` and honors a `Permission_Aware_Datapoint`'s callback on both branches, which is what keeps the submit reachable by a view-only user at all.  
* **The API layer caches `GET` responses in browser storage and caches no `POST`.** That is the wrong mechanism for either half of the insight — a `POST` is never cached, and the operation read's answer changes while its parameters do not — which is why the slice keeps its own entry instead.  
* **`API.set()` clears the datapoint's whole cache group after the request returns**, matching every stored key that *begins* with `modules::analytics-4::<datapoint>`. That is a correct default for a write and a hazard for this one, and it is what decides the name the slice's own entry is stored under — see [Browser-side caching](#browser-side-caching).  
* **`getItem` and `setItem` from `googlesitekit/api/cache` are the API cache's own storage, callable directly.** `modules/ads`'s `isWooCommerceRedirectModalDismissed()` resolver already reads one from inside a store. That is what the insight's cache entry is built from.  
* **A control is where a `setTimeout` belongs in a datastore**, the way `scheduleSyncAvailableCustomDimensions()` on `modules/analytics-4` already schedules a deferred fetch. That is what the [wait between reads](#waiting-for-the-generation) is built out of.  
* **`GoogleChart` passes Google Charts interval roles through untouched and renders `children` inside its own positioned wrapper.** The first is what draws the expected-range bands, the second is what the forecast's overlays and the marker tooltip hang off. Its `getChartOptions()` reinstating Google's own tooltip is the one behavior the charts work around — see [Chart construction](#chart-construction).  
* **`Custom_Dimensions_Data_Available`** answers server-side whether `googlesitekit_post_date` is gathering data, which decides whether the content markers are derived at all.  
* **`ThumbsSurveyTrigger` renders the "Tell us more" link only when `downvoteFormURL` is set**, which is what lets the feedback prompt beneath the insight carry no follow-up URL without a change to that component — see [Is this helpful?](#is-this-helpful).

The new infrastructure is the generative transport and the model in front of it: two `Google_Proxy` methods, two `Analytics_4` datapoints over a shared base, the baseline model, the payload builder, and the slice's insight resolver. The external dependencies the plugin has not talked to before are the service's `/v1/ai/benchmarking` endpoint and its `/v1/ai/operations/:operation_id` companion; the GA4 Data API and the Search Console API are reached the way they always are.

## **Detailed design**

### **Feature flag**

The epic is built behind a new `trafficInsights` feature flag.

The two insight sections, the chart's baseline columns and overlays, and the content-marker series are each rendered under the flag check, so with the flag off both tabs render exactly as they do today. The two insight datapoints are added to `Analytics_4::get_datapoint_definitions()` only when the flag is enabled, and `GET:benchmarking-data` derives the baseline, the forecast, the markers and the payload only under the same check — **with the flag off the data response is byte-identical to what it is today**, which is what keeps the existing browser cache entries valid across a flag change.

An issue to remove the flag and its conditional logic should be raised once the feature is stable at 100%.

### **Gating and visibility**

Two of the five additions carry their own gate, and the two gates are not the same:

1. **The insight and the baseline need thirteen months of property history.** The generative request compares the period against the same period a year ago and the year before that, and the baseline's full tier reads a year back for seasonality; neither is meaningful on a younger property. The condition is the one the Typical Traffic tab already applies — `getPropertyCreateTime()` on `modules/analytics-4` at least 13 months back — evaluated once and read by both tabs.  
2. **The content markers need no history at all.** A publish date inside the selected range is a publish date, so the markers render on a property connected last month.

**That difference makes the Traffic Overview tab request the data response even on a young property**, where before this epic it requested nothing from `GET:benchmarking-data` at all. Both panels now call the same selector with the same two dates, and the slice's fetch store answers both from one request, so the cost is one request per date range rather than one per panel — see [Panel data flow](#panel-data-flow).

The [baseline](#expected-baseline) and the history gate do not measure the same thing. The gate asks for 13 months from property creation; the baseline's full tier needs 392 days of history before the earliest day it judges. **That difference is one of precision, not of availability: whenever the band renders, it renders at whichever tier the history supports.** Short of the full tier the prediction drops annual seasonality and keeps the weekday shape; shorter still it falls back to a same-weekday average. Only the [look-ahead](#the-28-day-look-ahead) is withheld below the full tier, and for a reason of its own.

What is still open beneath the tiers is the floor — how little history, and how little daily volume, leaves a baseline worth drawing at all — see the [open question](#what-are-the-minimum-data-thresholds-for-each-level-of-analysis?).

### **Panel data flow** {#panel-data-flow}

Both panels call `useBenchmarkingData()` with the selected date range, and the [slice](#datastore-slice) answers both from one request keyed on the two dates. The Typical Traffic panel additionally calls `useBenchmarkingInsight()`, and so does Traffic Overview — the same two dates, the same resolved value.

**Both selectors take the same two dates**; that the insight request depends on the data request is the slice's business, not the panel's.

The data response gains five fields:

| Field | Shape | Rendered by |
| :---- | :---- | :---- |
| `dailyTraffic[].expectedMin`, `expectedMax` | bounds on each day inside the selected range; `null` on the earlier days of the thirteen-month window | the Typical Traffic chart's band |
| `forecast` | one row per projected day: `{ date, predicted, expectedMin, expectedMax }`; omitted below the full baseline tier | the Typical Traffic chart's forward half |
| `lookahead` | `{ direction, projectedVisitors, changePercent }`; omitted with `forecast` | `LookaheadSummary` |
| `baseline` | `{ periodMonths, expectedRangeMin, expectedRangeMax, trendDirection, statusVsBaseline, isSeasonalPeriod, tier }` | the `HISTORICAL_BASELINE` factor, and the band's copy |
| `contentMarkers` | `{ date, visitors, posts }` per publish date, already capped and ranked | the Traffic Overview chart's marker series |
| `payload` | one opaque string, never read in the browser | forwarded to `POST:benchmarking-insight` |

The insight response carries the five generated fields and nothing else:

| Field | Shape | Rendered by |
| :---- | :---- | :---- |
| `insight` | `{ scenario, topDimensions, text, driver, actionableRecommendation }` | `GeneratedInsight` on both tabs, and the order the key factors are walked in |

**Nothing on the data response depends on the insight, and the ordering the key factors want is the one thing that crosses between them.** `topDimensions` decides the order [Key factors](#insight-driven-factor-ordering) walks its sections in; it falls back to the order the response already carries while the insight is still generating, and stays on it if the insight never arrives. **A section must not appear, disappear or reorder when the insight lands mid-render** — it re-sorts, and the response's own ranking means the first ordering is a plausible one rather than a placeholder.

`forecast` is its own array rather than 28 more rows on `dailyTraffic`, and it is 28 rows at every date range the selector offers. **Every other consumer of `dailyTraffic` reads days that happened** — the chart's measured line, the period totals, the band — and a forward row carrying no `visitors` value would have to be filtered out by each of them. Keeping the two apart is also what lets the horizon stay fixed while the window behind it changes.

### **Chart construction** {#chart-construction}

Both charts gain work in this epic, and what they gain has three pieces in common. The [interval chart PoC](https://storage.googleapis.com/site-kit-github-artifacts/temp/performance-benchmarking-charts.html) is the reference implementation for all of it, drawn against the same chart version `GoogleChart` pins; the columns and the option values are in the [appendix](#chart-data-tables-and-options).

**A band is a pair of interval columns, not a second chart type.** Two columns declared with `role: 'interval'` follow the series they annotate, and `intervals` carries the fill: `style: 'area'`, the color and its opacity, and `lineWidth: 0` with `pointSize: 0` so the band is a filled region with no boundary lines or points along it. `curveType: 'function'` smooths the bounds with the same interpolation as the line, so the band follows the line's shape rather than reading as a separate envelope drawn near it. `getFilteredChartData()` passes the table through as long as no `selectedStats` is given, and `getChartOptions()` does not touch `intervals`, so **`GoogleChart` itself needs no change**. The exact colors are the design system's; what the chart code has to hold is the relationship — a band is a tint of the line it belongs to, and anything drawn behind a band is lighter than the band, or the band stops being readable where it matters most.

**The axis maximum is computed from the bands, not from the lines.** `viewWindow.max` has to clear the highest bound in the table — on a day the site underperforms, `expectedMax` sits well above the actual line, and in the [forecast](#the-28-day-look-ahead) it sits higher again by that week's widening factor. An axis fitted to the line clips the band drawn around it.

**Google's own tooltip is off, and the option that turns it off needs help.** `getChartOptions()` sets `tooltip.isHtml` and `tooltip.trigger: 'both'` on any `LineChart` whose options do not already declare `isHtml`, so passing `tooltip: { trigger: 'none' }` alone is silently overridden and the built-in tooltips come back. Both charts declare `isHtml` explicitly beside `trigger: 'none'`. What replaces it differs per chart: the Overview chart's marker tooltip, and the Typical Traffic chart's card over the projected region.

**Overlays are React children positioned in the `ready` handler**, from `chartWrapper.getChart().getChartLayoutInterface()` — `getChartAreaBoundingBox()` for the plot box and `getXLocation( date )` for a given day's x. The Typical Traffic chart already positions its selected-range region this way and already gives its wrapper the `z-index: 0` modifier that an overlay painting behind the plotted line needs; the forecast's divider and region tint reuse both.

**The legend becomes HTML rather than the chart's own.** `legend: { position: 'none' }`, and a `ChartLegend` component beneath each chart renders one item per visible series or band with a swatch matching its shape — a bar for a line, a block for a band, a dot for a marker. Google Charts cannot put a band in its legend at all, since an interval column is not a series; rendering the legend ourselves is also what lets an item appear only when its data does, so a chart with no forecast has no "predicted range" item explaining a band that is not there. `ChartLegend` and the tooltip card live under `traffic-overview/charts/` and are built by whichever of the two chart issues lands first.

### **Generated insight**

`GeneratedInsight` takes the resolved insight and a variant prop, and renders three distinct elements rather than one blob: `text`, the summary comparing the period against the baseline, under 250 characters; `driver`, the concrete explanation with the numbers in it, under 210 characters; and `actionableRecommendation`, the suggested next step, under 400 characters. The service enforces those character limits in the prompt rather than by truncation, so the layout treats them as the expected case and not as a guarantee.

The two variants are the two placements:

* **The teaser**, at the top of Traffic Overview ([Figma](https://www.figma.com/design/MWN8TXAjfTeKLF0DZ91bIX/Performance-benchmarking?node-id=740-40731&m=dev)), renders `text` alone with the scenario treatment. The Overview tab is the reading tab and the teaser is one sentence in it.  
* **The full block**, at the top of Typical Traffic ([Figma](https://www.figma.com/design/MWN8TXAjfTeKLF0DZ91bIX/Performance-benchmarking?node-id=552-10450&m=dev)), renders all three, which is where an explanation and a recommendation belong: the tab the reader opened to find out whether the period was normal.

The icon and emphasis treatment come from a lookup in `constants.ts` keyed by the eleven `scenario` codes, with a default entry that any unrecognized code falls through to. **The plugin never parses the localized text to decide layout** — that is exactly what `scenario` exists for, and a scenario the service adds later renders in the default treatment rather than in none at all. All three strings render as plain React children, per [Sanitization responsibilities](#sanitization-responsibilities).

**One generation serves both tabs.** The slice keys the insight by the date range and the locale, so switching tabs reads a resolved value rather than starting a second operation, and a later mount over the same range reads the [browser cache](#browser-side-caching) rather than the service. This matters — the service rate-limits to a burst of 10 with a refill of 2 per hour per site and user, and a generation it has not already cached costs one of those tokens.

The component has two absent states and they look different on purpose. While the generation is being waited on it renders `PreviewBlock` placeholders at its strings' heights, because an insight *is* coming and the block reserving its space is what stops the panel reflowing under the reader when it arrives. Once the insight is known not to be coming it returns `null`: no block, and no placeholder standing in for one.

### **Is this helpful?** {#is-this-helpful}

`InsightFeedbackPrompt` renders beneath the full insight block on Typical Traffic, with this widget's tracking event category and label. It renders the "Is this section helpful?" label, a `ThumbsSurveyTrigger` and the thank-you popper, and nothing else — there is no "Tell us more" link and no follow-up form URL, since `downvoteFormURL` is left unset.

**It renders only in the [Narrated](#insight-states) state**, beside the text it is asking about — there is nothing to rate while the insight is generating, and nothing left to rate once it is known not to be coming. This is the epic's post-launch quality signal for the narration itself: the vote leaves the plugin as `triggerSurvey( 'vote:<voteID>:<direction>' )` and the service stores that trigger against the user, which is what lets it survey the users who voted down — the follow-up is a survey the service serves back into the dashboard, not a link out of the plugin. The widget passes a vote ID of its own, so its votes aggregate separately and the follow-up reaches only the people the insight disappointed.

### **Content markers on the Traffic Overview chart**

The Overview chart gains a third column: that day's own visitor value on a day something was published, and `null` on every other day. Its series is drawn with `lineWidth: 0` and a point size, which puts a dot on the traffic line at the value the line already has there.

**Recently published content is a marker series rather than a `dateMarkers` line.** `GoogleChart`'s `dateMarkers` prop draws a dashed vertical line with an info icon — the right treatment for the single property-creation line the chart already uses it for, and the wrong one here. A dot reads as a point on the site's traffic where a full-height line reads as a boundary in it, and the marker needs a tooltip carrying a title, a date and a link, where a `dateMarkers` tooltip is one string.

Clicking a dot opens that tooltip, through the chart's `select` event on `onSelect`: a row that is not a marker dismisses it, a marker row highlights the dot and positions a card above it carrying the post title, its publish date and a link to the post. The card is positioned from the chart-area box and the axis range per the [shared construction](#chart-construction), and its copy is formatted in the browser with `sprintf` against translated patterns — neither the datapoint nor the service returns per-item strings. Google Charts has no border option for a point, so the `ready` handler gives the rendered marker circles a white stroke; that outline is what separates a dot from the line running under it.

`contentMarkers` is derived server-side from the same rows that become `recent_content_momentum` in the payload, so the dots and the narration describe the same posts. Four rules live in that derivation:

* The marker date is the post's publish date from `customEvent:googlesitekit_post_date`, the dimension those rows are already filtered on; the payload's `published_days_ago` is the same value counted back from the reference date.  
* Each marker carries its posts' titles alongside their URLs, because the tooltip's first line is the title. Both are already on the rows the response is built from.  
* The root URL and index pages are dropped, matching the exclusion the service applies before it narrates — a marker on `/` would annotate a page the insight has been told to ignore.  
* Posts sharing a publish date collapse into a single marker whose tooltip names them all, and the list is capped and ranked by visitor gain. One day is one x position and therefore one dot, and an uncapped list on a site that publishes daily turns the line into a scatter plot.

Where the custom dimension is absent or still gathering data, `contentMarkers` is empty, the marker column is not added, and the chart renders as it does today.

### **Expected baseline on the Typical Traffic chart** {#expected-baseline-on-the-typical-traffic-chart}

The Typical Traffic chart gains the expected range as a band under its measured line ([Figma](https://www.figma.com/design/MWN8TXAjfTeKLF0DZ91bIX/Performance-benchmarking?node-id=552-11363&m=dev)), and beyond `endDate` a dashed forecast line under a band that widens with distance.

**The band covers the selected date range, not the whole thirteen-month window.** Every day outside the selected range carries `null` in both interval columns, and `interpolateNulls: false` is what stops the band being drawn straight across them. Three reasons, in order of weight:

1. **The comparison the band supports is about the selected period.** The rest of the window is context for it, and shading a year of it says nothing a reader can act on.  
2. **The history it would need does not exist.** The full tier reads 392 days before the earliest day it judges; judging the earliest day of a thirteen-month window means reading more than two years back, which the 13-month history gate does not begin to guarantee. Bounding the band to the selected range bounds the lookback at `days_in_period + 392` — 420 days at the default range, 482 at the widest — which is a report the datapoint can actually run.  
3. **The chart already marks the selected range.** The band lands inside the shaded region that is there for exactly that purpose, so the two treatments reinforce one reading rather than competing.

Beyond `endDate` the projected series and its own interval pair carry the forward half. Interval columns bind to the series column they follow, which is what lets one table hold two bands whose widths are computed differently. Every projected cell is `null` on a measured row and every measured cell is `null` on a projected row, so one domain column carries two lines.

**The two halves meet at a point, not across a gap or a step.** The last measured row carries the projected series as well, and both its interval columns are set to that same actual value, so the band has zero width there and fans out from the point where the solid line ends: the last measured day is a measurement and carries no uncertainty of its own, while day \+1 already has [the widened band](#the-28-day-look-ahead) the model gives it. A divider marks that day, drawn as a one-pixel overlay at `getXLocation( endDate )`, and the projected half sits on a tinted background region running from the divider to the right edge of the plot area.

The projected line takes **the same color as the measured one**: what separates the halves is the dash pattern, the divider and the background tint, not a second hue, because the two are one series of the same quantity and only one of them has happened. The projected band is a step darker than the region tint behind it so that it stays legible against it. Hovering anywhere in the region reveals a card naming what the shaded area is and carrying the section's support link — the band being the least self-evident thing in the widget. The same link sits beside the legend, since the card exists only while there is a forecast to explain and the measured band needs explaining either way.

Building the table stays a pure function over the arrays, which puts the column order the interval roles depend on, the joining row, the null placement and the axis maximum under a unit test rather than under inspection.

Below the full [tier](#expected-baseline) the response carries no `forecast`: the table stops at the measured columns, the overlays and the projected legend items are not rendered, and the chart is the measured half alone — where the measured band itself still renders at whichever tier the property's history supports rather than disappearing along with the forecast.

The chart is unaffected by the generative call: `dailyTraffic`, `forecast` and `lookahead` arrive in the data response, which no proxy call sits in front of.

### **Look-ahead projection** {#look-ahead-projection}

`LookaheadSummary` renders directly beneath the chart as its caption, and says in one line what the next 28 days are projected to do: the `lookahead` object's `direction` selects the copy, and `projectedVisitors` and `changePercent` fill it in. `BOOM` and `LULL` each read as a heads-up; `STEADY` says the next four weeks look like an ordinary four weeks for this site, which is the honest answer on most days and therefore the one this section renders most often.

**The line is the plugin's copy, not the service's.** It is a translated pattern filled with `sprintf` over two numbers formatted through `numFmt`. The request payload carries no forward-looking field, so the service cannot state a projected figure even though `LOOKAHEAD` and `CURRENT_PERIOD_FORECAST` are in its scenario set — which is precisely what stops the two narrations contradicting each other on a number only one of them holds. Sending the projection instead is an [alternative we considered](#narrating-the-look-ahead-in-the-service-rather-than-in-the-plugin).

The component renders nothing when `lookahead` is absent. That is the same condition that drops the chart's forward half, so a site below the full baseline tier gets a measured chart and no caption dangling under it rather than an empty block.

### **Insight-driven factor ordering** {#insight-driven-factor-ordering}

The key factors section already walks a set of dimension codes in an order the response supplies, off a catalog in `traffic-overview/factors/registry.ts` that holds each code's copy, its `contextualData` key and its renderer. This epic changes where the order comes from and adds one entry to the catalog.

What the insight carries is `topDimensions`: up to three `DimensionType` codes — `TRAFFIC_CHANNELS`, `AUDIENCE_SEGMENTS`, `PAGES`, `SEARCH_QUERIES`, `DEVICES`, `CATEGORIES`, `REFERRING_SITES`, `HISTORICAL_BASELINE` — ordered from highest impact down, chosen by the model from `ranked_dimensions`, the plugin's own ordering forwarded in the [payload](#payload-assembly). **Those are the service's codes and not the catalog's**, so a map from `DimensionType` to catalog code lives beside the catalog: one entry per code, and an unrecognized `DimensionType` maps to nothing and is dropped, costing one section rather than the section list.

**The service's job is judgment, not arithmetic.** The plugin's own [Response assembly](./typical-traffic-design-doc.md#response-assembly) ranks the dimensions once; the service reasons from that ranking rather than deriving one of its own, and decides only which of the plugin's top-ranked dimensions actually explains the period well enough to lead the narration with.

Three resolution rules, on top of the two the lookup already applies:

* **`topDimensions` reorders; it does not filter.** The codes it names lead, in its order, and every other dimension the response carries follows in the response's own ranking. A model that names one dimension must not remove the other six from a section a reader has already seen.  
* **The fallback is the response's ranking**, which is what the section renders before the insight arrives and what it keeps if the insight never does. The section therefore renders complete from the first paint and re-sorts once `topDimensions` lands.  
* **A re-sort must not move focus**, since it happens seconds after the panel became interactive.

`HISTORICAL_BASELINE` is the new catalog entry and the one to handle deliberately. The service returns it when the site's own trajectory is the explanation, and it is also the fallback it substitutes when the model names no valid dimension at all — and unlike every other code it has no `contextualData` key behind it. Its catalog entry therefore reads the response's `baseline` object rather than a dimension array, and explains the movement against the expected band from `statusVsBaseline`, `trendDirection` and the `periodMonths` the tier actually used. **This is why a catalog entry carries a component rather than only a `contextualData` key.**

### **Expected baseline** {#expected-baseline}

The expected range is a stateless, closed-form calculation over the site's own daily `totalUsers` history, evaluated in PHP for every day of the selected range and for each of the 28 days projected beyond it. It stores nothing, trains nothing, and is arithmetic over a few hundred rows — negligible against the API calls in the same request.

#### *The daily series it reads*

The daily series the datapoint already runs supplies every input, with its window extended: from the thirteen months the chart plots to `days_in_period + 392` days ending on the selected range's `endDate` — 420 rows at the default 28-day range, 482 at the 90-day range. The 392 is the seasonality lookback: the earliest day the band covers reaches 364 days back for the same weekday a year ago, and a further 28 days for that day's own trailing four-week average.

**The chart still plots the trailing 395 of those rows**, and the lookback beyond them is consumed server-side and never crosses the wire. The response's `dailyTraffic` array does not change length.

The series is already zero-filled where GA4 returns no row for a day, which the derivation depends on: a missing day must not shorten a week or drag down a weekday average.

#### *The point prediction*

For a day `t`, three factors multiply into the prediction:

`predicted( t ) = daily_base_level * weekday_factor( weekday( t ) ) * annual_seasonality( t )`

1. **`weekday_factor`** — the site's stable weekday-versus-weekend shape, measured over the trailing 9 weeks (63 days): traffic on that weekday summed across the 9 weeks, divided by one seventh of the 63-day total. `1.00` is an average day, `1.20` is 20% above it. Nine observations of each weekday dilute single-day noise on a small site, and the 63 days are already in the report above.  
2. **`daily_base_level`** — the structural operating scale, from an exponentially weighted moving average over the 9 *whole-week* totals rather than the 63 individual days, which removes daily jitter and gives the level seven times the data density. `weekly_ewma( 1 )` is seeded with the mean of the first three weekly totals; each later week is clamped to `[ 0.5x, 2x ]` of the preceding `weekly_ewma` value before being folded in at `alpha = 0.20`; and `daily_base_level = weekly_ewma( 9 ) / 7`. The seed and the clamp are what hold a viral week to at most `+20%` of baseline movement and a tracking outage to at most `-10%`, and what make the result reproducible digit for digit in any other implementation of the same model.  
3. **`annual_seasonality`** — last year's calendar shape for the same weekday, `y( t - 364 ) / mean( y( t - 364 - 7k ) )` for `k = 1...4`. A Tuesday that ran 30% above its own four-week average during last year's Black Friday week scales this year's Tuesday by `1.30`. The offset is 364 rather than 365 because 52 weeks is exactly 364 days, which keeps the weekday aligned.

Trend is deliberately not a fourth factor: the EWMA level already carries the site's current scale, and multiplying it by a forward growth ratio would count that growth twice.

The trailing 63 days end on the selected range's `endDate`, so for the default range the days being judged are also inputs to their own baseline. Whether the window should instead end where the selected range begins is an [open question](#does-the-baseline's-trailing-window-include-the-days-it-judges?).

#### *The uncertainty band*

Day-to-day variance in visitor counts scales with the square root of the mean rather than linearly with it, so the band is scaled the same way, with an absolute floor:

* `ribbon_width( t ) = MAX( 5, 3 * SQRT( predicted( t ) ) )`  
* `range_min( t ) = MAX( 0, predicted( t ) - ribbon_width( t ) )`  
* `range_max( t ) = predicted( t ) + ribbon_width( t )`

Three standard deviations of Poisson count noise puts historical coverage near 95% at every site size, with no thresholds, tuning or visual step-changes, and the band is absolutely wider at weekday peaks while being proportionally tighter there:

| Daily prediction | Band | Effective percentage | What the site owner sees |
| :---- | :---- | :---- | :---- |
| `25` | `+/- 15` | `+/- 60%` | An honestly wide band that does not cry wolf over ordinary variance. |
| `400` | `+/- 60` | `+/- 15%` | A band that tracks normal operational fluctuation. |
| `10,000` | `+/- 300` | `+/- 3%` | A tight band that reacts to genuine structural moves. |

#### *Tiers when the history is short*

The model degrades in place rather than failing:

1. **Full model** — the year-ago window is covered, and all three factors apply.  
2. **No annual seasonality** — the property is younger than the seasonality lookback, so `annual_seasonality` is dropped and the prediction is `daily_base_level * weekday_factor`. This tier needs only 8 to 9 weeks of trailing data.  
3. **Fewer than 9 weeks** — the prediction for a day is the average of that same weekday across whatever weeks are available.

#### *The 28-day look-ahead* {#the-28-day-look-ahead}

The same three factors run forward. For a day `t` in the 28 days after the selected range's `endDate`:

`predicted( t ) = daily_base_level * weekday_factor( weekday( t ) ) * annual_seasonality( t )`

Two of the three are frozen and one is live. `daily_base_level` and `weekday_factor` hold at the values measured up to `endDate`, which makes the projection a "hold steady" statement about the site's operating scale; **feeding seasonally adjusted predictions back into the EWMA is what must not happen**, because a holiday spike folded into the level stays in it for months afterward. `annual_seasonality( t )` is computed exactly as it is for a judged day, since `t - 364` is in the past even when `t` is not. The curve therefore holds today's scale and takes last year's shape, which is the whole of what it claims.

The projection needs no additional report rows. Day \+28 reads `endDate - 336`, and day \+1's trailing four-week average reaches back to `endDate - 391` — both inside the 392-day lookback the earliest judged day already forces. The forward window starts the day after `endDate`, which at the dashboard's usual one-day offset makes the first projected day the current one.

Uncertainty grows with distance, so the band widens by 5% per week across the horizon, with `k = CEIL( days_ahead / 7 )` the week a day falls in:

`lookahead_ribbon_width( t ) = MAX( 5, 3 * SQRT( predicted( t ) ) ) * ( 1 + 0.05 * k )`

Day \+1 is drawn at `1.05x` the width an identical prediction would get today, and day \+28 at `1.20x`. No forward day is narrower than that, which is the failure mode worth naming: a forecast starting at zero width on day \+1 asserts perfect knowledge of tomorrow, and then makes ordinary day-+2 variance look like a broken model. The one point that does carry zero width is the last *measured* day, where [the chart](#expected-baseline-on-the-typical-traffic-chart) joins the two halves — a measurement, not a prediction.

The horizon is 28 days at every date range, and so is the comparison behind [the projection line](#look-ahead-projection): the 28 forward predictions are summed and tested against the 28 days of actuals ending on `endDate`, at a threshold of ±15%. Above it the next four weeks are a `BOOM`, below it a `LULL`, inside it `STEADY` — that direction, the projected total and the signed percentage are the three fields `lookahead` carries. Taking the comparison window from the selected range instead would put four weeks against thirteen on the 90-day view and report a lull on every site on earth, so it is fixed at 28 days on both sides.

**The look-ahead runs at the full tier only.** Below it there is no `annual_seasonality`, and the projection collapses to the weekday shape at today's level repeated four times — a line carrying no forward information, under a comparison decided by how unusual the last 28 days were rather than by anything about the next 28\. Where the tier is weekday-only or the same-weekday average, the response omits `forecast` and `lookahead` entirely. This is the one place the model does not degrade in place, and deliberately so: the degraded version is not a weaker forecast but a statement about the future assembled out of the recent past.

#### *What the service is told* {#what-the-service-is-told}

The baseline is not only drawn. The request payload carries a `baseline` object built from the same model, and it is what lets the narration say "compared to your site's overall baseline trend for the past X months" — and, more consequentially, what decides which scenario comes back: `BASELINE_GROWTH`, `STEADY_BUT_DRIFTING`, `CURRENT_PERIOD_FORECAST` and `STEADY_STATE` are each selected off these fields. **The baseline model is therefore an input to the insight, not just to the chart**, which puts it upstream of payload assembly rather than beside it.

| Field | Derived from |
| :---- | :---- |
| `period_months` | The history the tier actually used, in whole months — 14 for the full model at the default range, 2 for the weekday-only tier. A `baseline` whose `period_months` is not positive is rejected with a 400, and the narration quotes the number back to the user. |
| `expected_range_min`, `expected_range_max` | Integer bounds on the period *total*, the same scale as `visitors[ 0 ].current`, not on a single day. |
| `trend_direction` | `UP`, `DOWN` or `STABLE` over the baseline period, from the `weekly_ewma` trajectory. |
| `status_vs_baseline` | `ABOVE_EXPECTED`, `WITHIN_EXPECTED` or `BELOW_EXPECTED`, from where the period's actual total falls against those bounds. |
| `is_seasonal_period` | Whether `annual_seasonality` moves the expectation materially across the judged window. The service will not use the word "seasonal", in any language, unless this is true. |

The period-total bounds are not the daily bounds added up. Variance adds across days while the band scales with a square root, so summing 28 daily `3 * SQRT` bands overstates the period band several times over and would report almost every period as `WITHIN_EXPECTED`. The band has to be recomputed at period scale from the summed prediction, and neither that multiplier nor the thresholds behind `trend_direction` and `is_seasonal_period` follow from the daily model — see the [open question](#how-is-the-baseline-payload-object-derived?).

`baseline` is optional, and omitting it degrades the narration rather than breaking it: the service compares against the previous period instead and says so. The short-history tiers therefore have a real choice — send a weekday-only baseline with `period_months: 2`, or send none.

#### *Where the code lives*

The computation is one class under `includes/Modules/Analytics_4/Benchmarking/`, `Expected_Baseline`, taking the zero-filled daily series and the judged range and returning the per-day bounds, the forward projection and the period-scale `baseline` fields. The three factors, the band, the tier selection and the forward loop are methods on it with no dependencies beyond the rows — no settings, no options, no service client — so the whole model is unit-testable against fixed daily fixtures in PHPUnit.

Its result feeds three consumers in the same request: the bounds on the `dailyTraffic` rows, the `forecast` rows and `lookahead` object it continues into, and the `baseline` object the payload carries to the service. The reference date comes from the same `Context` the rest of the datapoint uses, which is what keeps the fixtures deterministic — a projection is a claim about days that have not happened, so a model reading the clock itself would have no fixed answer to assert against.

### **Generative endpoint infrastructure**

#### *Google\_Proxy additions* {#google_proxy-additions}

`Google_Proxy` gains two URI constants and two public methods. The first posts the payload to the generative endpoint through `request()` with `json_request => true` and `async` set, and returns whichever answer comes back — an operation ID, or the finished insight. The second reads an operation back by ID. The credentials and bearer header the helper already sends are exactly what both endpoints validate: a mismatched `site_secret` is a 400, and an unverifiable token or a user not registered against that site is a 403\.

**The operation read is a `GET`, and `request()` cannot make one.** It calls `wp_remote_post()` unconditionally and injects `site_id`/`site_secret` into the request body, where the operations endpoint reads them from the query string. `request()` therefore gains an optional method argument: given `GET` it sends through `wp_remote_get()` and moves the credentials and any body arguments into the query string, and absent the argument it behaves exactly as it does today. **This is a change to a Core class every proxy call in the plugin goes through**, which is why it is additive by construction rather than a refactor of the existing path — the response handling, the JSON decode and the `WP_Error` shaping stay in one place, which is the reason for extending the helper rather than writing a second one beside it.

The user's locale travels as an `hl` **query parameter** on the submit, which is the only place the service looks for it — not a body field. It is resolved from `$this->context->get_locale( 'user' )` by the datapoint that assembles the payload, carried inside the [encoded payload](#wire-format-additions), and put on the URI by the submit method. The locale is part of the service's own cache key, so switching the admin language produces a newly generated insight rather than a cached one in the wrong language.

#### *Insight datapoints* {#insight-datapoints}

Two datapoints, one per service call, over an abstract base that holds everything they share — the layout `Site_Goals_Settings_Datapoint` already gives `Get_Site_Goals_Settings` and `Save_Site_Goals_Settings` on this same module:

1. **`POST:benchmarking-insight`** takes the opaque string the data response carried, as `payload` inside the request body's `data` object — the shape the `EDITABLE` route declares and `Module::set_data()` casts. It decodes and validates it, then submits it with `async` set. Two things can come back: **the finished insight**, when the service's 24-hour response cache already holds a generation for this payload, since its pipeline consults that cache before anything else; or **an operation ID**, when the cache missed and the service has started generating in the background.  
2. **`GET:benchmarking-insight`** takes an `operationID` an earlier submit returned, reads that operation back, and returns its state — the insight on success, the service's error on failure.

**The payload travels in a request body, and that is the reason for the verb.** A `GET` has to carry it in the query string, where an encoded payload of a couple of kilobytes runs against the roughly 8 KB a default Apache or nginx allows on a request line, and against the lower figure some WAFs enforce — a transport ceiling that would have failed on exactly the sites whose page titles and search queries run longest. A body has no such ceiling. **The verbs now match the calls they carry**, too: the submit `POST`s a submit, the read `GET`s a read, and neither datapoint has a parameter that is an alternative to another one. The two cases this reverses are under [A `GET` submit carrying the payload in the query string](#a-get-submit-carrying-the-payload-in-the-query-string) and [Keeping the submit and the read on one datapoint](#keeping-the-submit-and-the-read-on-one-datapoint).

**A write datapoint has to declare its own permission check, and here that is load-bearing rather than tidy.** The `EDITABLE` branch of the module data route defaults to `MANAGE_OPTIONS` where the `READABLE` branch defaults to the view-insights capability, so a submit declaring nothing would be reachable by administrators and by nobody else — every number and no narration in the view-only dashboard, and a widget that silently loses its point for the users it was shared with. Both insight datapoints declare the dashboard-viewing capability, and the shared base is where they declare it once.

Both resolve a bearer token from the same `OAuth_Client` the reports run under — the caller's own on a normal request, the module owner's on a shared one — through a closure in the definition array, so the resolution rules stay in `Module`. Being `Shareable_Datapoint`s is all either needs for that. Where no token resolves at all, a disconnected owner being the case that produces it, the call is not attempted rather than made to be refused.

**A failure is a `WP_Error`, not a field on a 200\.** The numbers have already arrived in their own response, so there is nothing for a success status to protect, and the widget reads the failure off the error store instead of off a reason field:

| Failure | Datapoint | Status | What the slice does |
| :---- | :---- | :---- | :---- |
| Rate limit exhausted | submit | `429` | Stops, and holds the failure for an hour — the time the limiter takes to refill two tokens |
| Token unverifiable, or user not registered | either | `403` | Stops, and holds the failure for an hour |
| Operation gone or expired | read | `404` | Discards the handle and submits again, once |
| Operation `FAILED` | read | `500`, carrying the service's code and message | Stops immediately, and holds nothing |
| A 5xx or a transport error | either | `500` | Stops, and holds nothing — the next mount tries again |

**A failed generation is visible rather than inferred**, which is the main thing the read buys beyond cost: the operation says `FAILED`, so the widget reaches its Reporting state at once instead of waiting out a deadline that cannot distinguish a failure from a slow model.

The service's 429 carries no `Retry-After` header, so the hour is the plugin's own figure, taken from the refill rate rather than read off the response. **Where those failures are held is the slice's own cache entry**, not the API layer's — see [Browser-side caching](#browser-side-caching).

#### *Waiting for the generation* {#waiting-for-the-generation}

The wait lives in the datastore, not in PHP. The [slice](#datastore-slice) submits once, keeps the operation ID it gets back, and reads that operation from a control that wraps `setTimeout` — the way `scheduleSyncAvailableCustomDimensions()` already schedules a deferred fetch on `modules/analytics-4` — until it is done.

Four properties the wait depends on:

1. **Nothing the API layer would cache may be cached.** An operation's state changes while its parameters stay the same, so a cached "not ready" would be replayed for an hour and the loop would never finish. The read therefore runs with caching off, and the slice caches the *outcome* instead. **Half of that hazard is structural**: the API layer caches no `POST`, and `API.set()` disables caching for the submit whether or not anyone asks it to.  
2. **Reading an operation costs no rate-limit token, and starts no work.** The limiter is consumed once, on the submit; the operations endpoint has no limiter in front of it at all. **This is why the submit happens exactly once** — the interval is a pure latency choice, and a tight one costs a memcache lookup per tick rather than a token and a duplicate generation.  
3. **The deadline bounds the wait, not the work.** Giving up leaves the generation running service-side, and its result lands in the service's 24-hour cache when it finishes, so the next mount over the same date range collects it in one submit that the cache answers for free. **A timeout is a rendering decision, not a lost generation.**  
4. **An operation lives for one hour** in the service's memcache, refreshed when its worker finishes. Past that a read is a 404, which the slice treats as a stale handle: it discards it and submits again, once, rather than alternating between the two datapoints indefinitely.

No PHP request in this design is long. The submit returns as soon as the service has created the operation, and each read is one fast `GET`, so `Google_Proxy::request()`'s 15-second default timeout is comfortable for both and neither `max_execution_time` nor a reverse proxy in front of the site constrains anything.

The interval, the deadline and whether the interval widens are an [open question](#what-is-the-cadence-and-deadline-for-waiting?) — a function of how long the service's generation actually takes, which the plugin can measure only against the live endpoint.

#### *Payload assembly* {#payload-assembly}

The request payload is `end_date`, `days_in_period`, a `visitors` array of `{ current, previous }` pairs — index 0 the active period this year, index 1 the same period a year ago, index 2 two years ago — `ranked_dimensions`, a `contextual_data` object with seven optional keys, and the optional `baseline` object. Every field is derived in PHP by the data datapoint, from the reports it has just run, and encoded into the string the browser hands back to [`POST:benchmarking-insight`](#insight-datapoints) unread:

| Payload field | Derived from |
| :---- | :---- |
| `end_date`, `days_in_period` | the `startDate` and `endDate` request parameters |
| `visitors` | the daily series for this year's two windows and last year's, and a new two-years-back totals report |
| `ranked_dimensions` | the dimension order already derived for the [key factors section](./typical-traffic-design-doc.md#response-assembly), translated into the service's `DimensionType` codes through the [dimension code mapping](#dimension-code-mapping) |
| `baseline` | `Expected_Baseline`'s [period-scale output](#what-the-service-is-told) |
| `recent_content_momentum` | the `pagePath` report filtered on `customEvent:googlesitekit_post_date` |
| `traffic_channel_surges` | the `sessionDefaultChannelGrouping` report |
| `category_resonance` | the `customEvent:googlesitekit_post_categories` report |
| `search_query_shifts` | the contextual-data filter's rows |
| `user_mix_shifts` | the `newVsReturning` report |
| `device_shifts` | the `deviceCategory` report |
| `referrers` | the `sessionSource` report |

**Seven of the rows are ones the datapoint already derives**, ranked and capped, for the key factors section; the payload takes them and renames them to the service's keys. `ranked_dimensions` is the same derivation's ordering, not a new one — it is the `dimensions` field the data response already carries, re-expressed in the service's own codes. Only three fields are new work: the two-years-back `visitors` entry, which needs its own totals report and takes the report count from seven to eight — still two batched calls; the `baseline` object; and the encoding.

The prior-year windows are both windows shifted back 364 days per year, the same offset the [baseline](#expected-baseline) uses to keep weekdays aligned. The first two `visitors` entries are summed out of the daily series, which now spans `days_in_period + 392` days at every range the selector offers; only the two-years-back entry falls outside that span.

Every dimension except `recent_content_momentum` and `category_resonance` carries a `{ current, previous }` pair rather than a single figure — the model's narration names actual numbers, and a pair is what lets it say the site's Organic Search visitors rose from one figure to another rather than only that they rose. **The ordering travels separately.** `ranked_dimensions` carries the plugin's own impact ranking — the same one [Response assembly](./typical-traffic-design-doc.md#response-assembly) computes for the key factors section — so the model never does arithmetic, and never reasons from an ordering a reader would not also see on Typical Traffic. A dimension with no place in `ranked_dimensions` is a dimension the narration cannot weigh against the others.

Which keys are present constrains which scenario can come back: `SEARCH_QUERY_SHIFTS`, `TRAFFIC_CHANNEL_SURGES`, `CATEGORY_RESONANCE`, `REFERRING_SITE_SHIFTS` and `RECENT_CONTENT_MOMENTUM` are each conditional on their own key being sent, so a site missing the custom dimensions is steered toward the baseline and steady-state scenarios rather than getting a worse version of the same insight.

Only `end_date`, `days_in_period` and a non-empty `visitors` are required; a request missing any of them is rejected with a 400\. The plugin assembles the payload itself, so a 400 is a plugin defect rather than a state a site can get into — but the payload passes through the browser on its way out, which is why `POST:benchmarking-insight` validates the decoded structure against these fields before forwarding it rather than trusting what it was handed. What that validation is defending against is set out under [Sanitization responsibilities](#sanitization-responsibilities).

The assembly has to be deterministic — stable ordering, stable rounding, stable row caps — because the payload is what the service keys its own 24-hour cache on, so two runs over unchanged figures that serialize differently are two generations.

#### *Wire format additions* {#wire-format-additions}

The response's positional format gains members for the baseline bounds, the forecast rows, the look-ahead, the period-scale baseline object and the content markers, and one member that is unlike every other: **the payload, as one opaque string.**

Two rules the existing format already fixes carry the additions without change — a missing value is `null` rather than `0`, and every non-integer arrives already rounded, so the bounds and the percentages need no new convention. The version integer that leads the envelope increments, and a decoder that does not recognize it treats the response as unusable rather than guessing.

**The payload is opaque to the browser.** It is JSON of the same positional shape, base64url encoded, and it crosses the browser as one string field — read off the data response, put on the submit's body, never parsed. **The browser encodes nothing**, so the two-sided contract stays a decoder in JS against an encoder in PHP rather than a codec on each side.

**The payload round-trips byte for byte, and that is load-bearing twice.** The service keys its own 24-hour cache on the serialized payload, so a browser that re-encoded would produce a new key — and a new generation — from an unchanged payload the moment JS and PHP disagreed about how to print a float. And the row caps, the ranking and the rounding stay decided in one language. Holding the payload as one opaque string rather than a structure is what makes both guarantees free: there is no point at which the browser *could* re-serialize it. The cost is that the fields the payload carries appear twice in the data response, once positionally for the widget to draw and once encoded for it to forward — a couple of kilobytes, and the cheapest part of this design.

base64url is what makes it an ASCII string that needs no escaping inside a JSON body and that nothing between the two ends can reformat. Deflating before encoding remains available where zlib is, signalled by a one-character prefix so the decoder needs no configuration to read either form.

The shared fixture both test suites already read gains the new members, and one case beyond them: a payload that survives a PHP encode, a JSON round trip and a PHP decode byte for byte — the property both cache keys depend on.

#### *Browser-side caching* {#browser-side-caching}

**Nothing is cached on the server.** The browser is where the caching lives — in the same storage the API layer uses, through two mechanisms rather than one:

| Entry | Managed by | Key | TTL |
| :---- | :---- | :---- | :---- |
| Data | the API layer, automatically | the two dates | `HOUR_IN_SECONDS` |
| Insight | the [slice](#datastore-slice), explicitly | the two dates and the locale | `DAY_IN_SECONDS`, or `HOUR_IN_SECONDS` for a held failure or a pending operation |

**The insight's entry has to be the slice's own, because the API layer cannot hold either half of the call.** The submit is a `POST`, which the API layer never caches; the operation read is a `GET` whose answer changes while its parameters do not, so caching on its parameters would replay a "not ready" until it expired. The slice writes the *outcome* through `getItem`/`setItem` instead, which is what lets it choose the key as well as the lifetime — and the key is the part the API layer could never have chosen.

**That entry is stored outside the datapoint's own cache group, and it has to be.** `API.set()` calls `invalidateCache( 'modules', 'analytics-4', 'benchmarking-insight' )` once the submit returns, and the match is by *prefix*: every stored key beginning `modules::analytics-4::benchmarking-insight` is deleted, a longer name extending it included. The slice therefore stores under a group of its own — `benchmarking-generated-insight`, keyed on the two dates and the locale — so that a submit for one date range cannot delete the insight held for another. **The current order of operations hides this rather than preventing it**, which is why it is worth stating: the invalidation runs before the resolver writes anything, so a colliding key would survive the write that follows it and be destroyed by the next range's submit instead — a miss that looks exactly like an expiry.

**The same entry holds a pending operation, and that is what makes a reload cheap.** While the wait is running, the entry carries the operation ID under the same key with the hour an operation lives for. A reload therefore reads the handle back and resumes the poll rather than submitting a second time — which matters because a second submit during a generation is the one call in this design that would both spend a token and duplicate a model run. The insight replaces the marker when it arrives.

**Given the choice, the key is the date range and the locale rather than the payload**, and the lifetime is a day rather than an hour. The payload changes whenever the figures move, which is hourly, and every changed payload is a fresh key on the service's cache as well — so keying on it would mean a new generation every hour for every date range anyone opens. Keying on the range means one generation per range per locale per day. **The entry survives a movement in the figures outright**, with no threshold treating a large enough swing as a miss: a threshold would buy back accuracy at the price of a generation on every still-open day it fires, and a swing that large is the thing the narration is describing. Keying on the payload instead is [an alternative](#keying-the-insight-cache-on-the-payload-instead-of-the-date-range) rather than a constraint.

A held failure is a marker under the same key with its own hour, so a 429 or a 403 stops the widget re-submitting on every mount without suppressing a retry for the rest of the day. A 5xx holds nothing.

What the browser cache is, precisely: `sessionStorage` in preference to `localStorage`, under a key prefix hashing the plugin version, the WordPress user's login, their auth session token and the blog ID. **So it is per user, per login session, and in practice per tab.** It absorbs remounts, switching between the two panels, and reloads in the same tab; a second tab starts empty.

**A second tab is not a second generation, though — only a second submit.** The service's cache is what covers everything the browser's does not: it holds a generation for 24 hours under the site, the identity, the end date, the locale and the payload, and its pipeline consults it before the rate limiter, so a submit it can answer costs neither a token nor a model call. The plugin's entry saves the round trip; the service's saves the generation. **That ordering is what makes this design affordable** — see [Dependencies](#dependencies).

What still costs a token and a generation is a payload the service has not seen: a new date range, a new locale, the hour rolling over on a range whose browser entry has expired, or a second admin whose identity is part of the service's cache key. **Waiting costs neither.** Against a burst of 10 refilling at 2 an hour, that is comfortable for the shapes real dashboards have. **A shared dashboard is the case that could have been alarming and is not**: every viewer's request runs under the owner's identity, so they all hit the same service cache entry, and fifty viewers of one date range cost one generation and — after the first — no tokens at all.

#### *Datastore slice* {#datastore-slice}

The `benchmarking` slice gains two `createFetchStore`s beside the one it has:

| Fetch store | Params | Request |
| :---- | :---- | :---- |
| `submitBenchmarkingInsight` | `{ payload }` | `API.set`, which is uncached by construction |
| `getBenchmarkingOperation` | `{ operationID }` | `API.get`, `useCache: false` |

**The second is named for what it reads rather than for the datapoint it calls.** `GET:benchmarking-insight` answers it, but a fetch store called `getBenchmarkingInsight` would record its errors under the same selector name the public selector uses, distinguished only by having different arguments — a distinction that is real, invisible in a test failure, and not worth relying on.

**The public selector takes the two dates, and neither the payload nor the operation ID.** Those are transport details — the arguments a request carries, not the identity of what is being asked for — and keeping them out of the signature is what stops every component and every test having to hold an opaque string to ask a question about a date range.

* `getBenchmarkingInsight( startDate, endDate )` — the five generated fields. Its resolver is where everything about the generative call lives, and it runs in this order:  
  1. Read the slice's [own cache entry](#browser-side-caching) for these dates and this locale. An insight ends it here; a failure marker is re-raised on the error store without a request; **a pending operation ID resumes at step 4\.**  
  2. Resolve `getBenchmarkingData` for the same dates, and take `payload` and `locale` off it. A data request that failed leaves the insight unresolved rather than in error, since there is no payload to submit.  
  3. Submit the payload. **An insight ends it here** — the service answered from its own cache, and the resolver receives the generated fields into the store exactly as it would at the end of a wait, rather than storing a handle it would then have to poll for something it already has. An operation ID instead is kept in slice state *and* written to the cache entry as a pending marker, so a remount resumes and so does a reload.  
  4. Wait, then read the operation, until it is done — see [Waiting for the generation](#waiting-for-the-generation). A 404 discards the handle and returns to step 3, once.  
  5. Write the outcome to the cache entry, replacing the pending marker: the insight for a day, a `429` or `403` as a marker for an hour, a failed operation or a 5xx not at all.  
* `isLoadingBenchmarkingInsight( startDate, endDate )` — resolution state and **both** insight fetch stores' in-flight flags. It stays `true` across the whole wait — the intervals between reads included, when neither store is in flight. That is what the [Generating](#insight-states) state reads, and reading one flag rather than both is the mistake that makes the state flicker once per tick.  
* The insight's failure is the error store's: `getErrorForSelector( 'getBenchmarkingInsight', [ startDate, endDate ] )` carries the status the [datapoints](#insight-datapoints) returned. **Two fetch stores mean two internal error keys**, each under its own base name and its own arguments, and the resolver is what normalizes whichever fired onto the one key the panels read. A deadline that passes is recorded there too, so a panel has one place to read from rather than a status field beside an error.

**The operation ID lives in slice state as well as in the cache entry, and the two are not redundant.** State is what a remount inside one page load reads, and it is where the resolver's own loop holds the handle between ticks; the cache entry is what survives a reload. Only the second needs a lifetime, and it takes the hour an operation lives for rather than the day an insight does.

**The locale reaches the slice as a field on the data response**, resolved server-side from `Context::get_locale( 'user' )`, rather than being derived in JS. The store therefore keys on the same locale string the proxy puts on the `hl` parameter, and the two cannot disagree.

`clearBenchmarkingData()` grows to drop the insight as well: the stored value, any held operation ID, and the slice's own insight entry through `deleteItem` — which the submit's own invalidation deliberately does not reach, so clearing it is something only this action does.

#### *Sanitization responsibilities* {#sanitization-responsibilities}

The service owns prompt-injection defense: the data travels inside `<data>` delimiters with an explicit instruction not to obey anything inside them, page titles and query, channel and category names are stripped of markup, titles are truncated to 100 characters, and the model's JSON output is schema-checked and run through narrative safety checks for HTML, script, `javascript:` and `data:` URIs and system-instruction overrides before it is returned. An unknown `top_dimensions` value is dropped server-side, and an empty list becomes `[ HISTORICAL_BASELINE ]`. A response that fails any of those checks is a 500, not a degraded insight. The plugin's responsibility is narrower but real, and the payload's route out through the browser adds to it:

* **Validate the decoded payload, do not trust it.** The string [`POST:benchmarking-insight`](#insight-datapoints) receives came from a datapoint of the plugin's own, but it arrives over a REST request an authenticated dashboard user can write by hand. The datapoint therefore checks the decoded structure the way it would check any request body: known fields only, the row shape each dimension declares, scalars cast, string lengths and row counts held to the same caps the encoder applies. **Without that check, any user who can view the dashboard can put text of their choosing into a model prompt** — bounded in what it can achieve by the service's own delimiting and output validation, and by the fact that the narration comes back only to them, but not a surface to leave open.  
* Send structured fields only. The payload carries no free-text field a site visitor could reach, and the browser adds nothing to what the data datapoint derived.  
* Treat `text`, `driver` and `actionableRecommendation` as untrusted text on render — plain text into a React child, never `dangerouslySetInnerHTML`. All three are model output.  
* Treat `scenario` and every `topDimensions` entry as enums and fall back to the default treatment for an unrecognized value, rather than assuming the server-side validation is the only gate.  
* Pass the service's response fields through the datapoint unchanged. The insight response is the plugin's own shape, but the three strings inside it are the model's, and re-encoding or wrapping them in PHP would only hide that from the components that have to treat them as untrusted.

### **Insight states** {#insight-states}

The data response decides whether a panel renders at all; the insight decides only whether its insight block does. **Two requests mean two independent state axes**, and every state below sits inside the panel's existing Ready state.

![][image2]

* **Generating** — the payload has been submitted and the generation is being waited on. The insight block renders its own `PreviewBlock` in place of the text; nothing else waits on it. **This is the state the widget spends its first few seconds in, and every number is already readable in it.**  
* **Narrated** — the operation succeeded, the insight is rendered, and the key factors have re-sorted onto `topDimensions`.  
* **Reporting** — the insight is not coming: a 429, a 403, a failed operation, or the [waiting deadline](#waiting-for-the-generation) passing. The insight block is absent, the key factors stay on the response's own ranking, and everything else stays exactly as it was in **Generating**.

**A failure of either insight datapoint never reaches a panel's error state.** That state belongs to the data request, and the two requests are independent, so there is no ordering to get right and no field to remember to omit.

What the Reporting state says in place of the insight, and whether a 429 says something different from a failure, is an [open question](#what-does-the-widget-show-when-the-rate-limit-is-hit?).

### **Architecture requirements**

New front-end code joins the existing widget directory: `assets/js/modules/analytics-4/components/traffic-overview/` gains `GeneratedInsight`, `LookaheadSummary` and `InsightFeedbackPrompt` under `components/`, `ChartLegend` and the tooltip card under `charts/`, the `DimensionType` map and the `HISTORICAL_BASELINE` entry under `factors/`, one hook, and the new field indices in `constants.ts`. Components are TypeScript function components, one component per file, with co-located tests and Storybook stories.

New PHP lives in two places: `includes/Modules/Analytics_4/Datapoints/` for the two insight datapoints and the abstract base they share, and `includes/Modules/Analytics_4/Benchmarking/` for `Expected_Baseline`, the payload builder and the wire format's new members. The two transport methods and the `GET` capability the second needs are added to `includes/Core/Authentication/Google_Proxy.php`.

Two existing files change outside this feature, and they are the ones to call out at review: `Analytics_4::get_datapoint_definitions()` gains the two datapoints under the flag check, and `Google_Proxy::request()` gains its optional method argument — the latter being the one every other proxy call in the plugin passes through.

### **REST infrastructure**

Two new datapoints, no new route. `GET:benchmarking-insight` is dispatched by the `READABLE` branch of the module datapoint route like any other read, `POST:benchmarking-insight` by the `EDITABLE` branch like any other write, and each implements `Permission_Aware_Datapoint` for its own permission check — which on the `EDITABLE` branch is what keeps the submit reachable by a view-only user at all.

**A widget load becomes two requests plus one per tick of the wait**, and only the first is expensive. The rest exist because generation is asynchronous, and each is one short proxy call — the round trips are not the cost worth managing, the reports and the generations are.

Three caches sit between the widget and the model. The [API layer](#browser-side-caching) holds the data response for an hour under the two dates. The [slice](#datastore-slice) holds the insight for a day under the two dates and the locale. The service holds a generation for 24 hours under the site, the hashed user, `end_date`, the locale and the serialized payload — and **consults it before its rate limiter**, which is the fact the whole economics of this design rests on.

## **Common considerations**

### **Dashboard sharing** {#dashboard-sharing}

The widget follows the existing Dashboard Sharing rules for the `analytics-4` module. The data datapoint already decides whether a view-only user sees a widget at all; the two insight datapoints decide only whether that widget is narrated. **All three are shareable, and a view-only user gets the whole widget, insight included.**

None of them needs a branch for it. The reports and the generative call both run under the module owner's credentials, so the service sees the owner — a user it can verify and one registered against the site. `Module::set_data()` dispatches through the same `execute_data_request()` path as `get_data()`, so the submit delegates to the owner's client on a shared request exactly as the reads do; being a write changes the permission default, not the token resolution.

**Nothing is pooled across users, so no sharing boundary runs through a cache.** Every stored copy of every response lives in the requesting user's own browser, behind a key prefix that hashes their login and session. A view-only user with Analytics but not Search Console shared assembles a payload without `search_query_shifts` and is served the narration built on that payload and no other.

**What is shared is the owner's identity at the service, and that turns out to help rather than to hurt.** Every viewer's submit is charged to the same site-and-owner rate-limit bucket — but it also resolves to the same key on the service's response cache, which its pipeline consults before that bucket. So the first viewer of a date range causes one generation and spends one token, and every viewer after them is answered out of that cache for free. **Fifty viewers of a shared dashboard cost what one costs.**

The alternative — shareable datapoints that skip generation on a shared request — is discussed under [Alternatives considered](#skipping-generation-on-a-shared-request).

### **Tester plugin** {#tester-plugin}

The states that matter for QA are hard to produce on a real site: a property with a genuine seasonal pattern, a rate-limited response, a generation slow enough to run past the waiting deadline, and each of the eleven `scenario` codes with each combination of `topDimensions` that drives a different ordering.

Two filters cover almost all of it, one per response, and each is the single point where the states behind it are reachable:

1. **Over the data response.** A short-history baseline tier, each of the three [look-ahead directions](#the-28-day-look-ahead) — which otherwise need a property whose last year happens to contain a seasonal swing in the right place — a period that falls above, inside and below the expected range, and content markers on a chosen set of days including two posts sharing one.  
2. **Over the insight response.** An arbitrary `scenario`, `topDimensions`, `text`, `driver` and `actionableRecommendation` without calling the service, and the failure modes beside them: a 429, a 403, and a 500\.

**Asynchronous generation is the part the tester plugin makes newly easy, and it needs its own control.** Answering the submit with an operation ID and then reporting that operation as running for a chosen number of reads is what exercises the **Generating** state; letting it run indefinitely is the deadline, and the case the deadline was written for — a give-up followed by a later mount that collects the finished generation in one submit the service's cache answers. Two more states are only reachable this way: an operation reported `FAILED`, which must reach the Reporting state at once rather than at the deadline, and a read answered `404`, which must produce one fresh submit rather than a loop.

Reaching any forced state needs the browser cache out of the way — both entries, since the insight's is the slice's own rather than the API layer's. `setUsingCache( false )` covers the data response; the insight's entry needs `clearBenchmarkingData()` or a cleared session store. **There is nothing to clear on the server.**

The view-only path needs its own coverage, since it is the same path rather than a reduced one: a shared request generating under the owner's token, and a shared request whose caller cannot read Search Console data, against the same date range.

### **Site Health**

The epic adds no debug fields. Nothing about the feature is persisted — no last-outcome record, no timings, no cached entry on the server — so there is nothing a debug field could read that a support engineer could not reproduce by loading the dashboard.

### **Feature Discovery**

The insight is introduced by nothing — no notification, tour step or badge. It appears at the top of two tabs the reader already opens, and its first paint is a `PreviewBlock` followed by a sentence, which is its own introduction.

The one thing that does need saying in the product is that the text is AI-generated, and that is a support link rather than a discovery surface — see [Documentation in-product](#documentation-in-product).

### **Internal Measurement: GA4 Events**

The epic adds no tracking events of its own. The feedback prompt beneath the insight emits the thumbs events `ThumbsSurveyTrigger` already emits, unchanged, and whether a 429 is tracked as an error is part of [the rate-limit question](#what-does-the-widget-show-when-the-rate-limit-is-hit?) rather than a measurement plan of this epic's.

### **Internal Measurement: Feature Metrics**

None. Nothing persists the outcome or the timings of a benchmarking request, which is the same reason there are no debug fields, and the feedback vote is the service's telemetry rather than a plugin metric.

## **Alternatives considered**

### **Folding the generative call into the data datapoint**

The insight could come back on `GET:benchmarking-data`: one datapoint that runs the reports, derives the payload, calls the generative endpoint and returns everything in one response. It is one request from the browser, one cache key, one loading state, and the browser never sees the payload at all.

We split it. Three reasons, and the first two are structural:

* **A response holding both cannot be cached usefully.** The figures are worth an hour and a generation is worth a day; one entry means the narration expires with the numbers and every hour costs a rate-limit token per date range, or the numbers go stale to keep the narration.  
* **One PHP request would hold a report call and a model call together**, against `max_execution_time` on shared hosting and whatever a reverse proxy in front of the site allows — and a request cut short by either is indistinguishable from a service failure.  
* **A failed narration would take the numbers with it**, or would have to be carefully arranged not to. Two responses make that structural instead: no failure of the generative endpoint can reach a response it is not in.

What the split costs is the payload's route out through the browser — a REST parameter a dashboard user can write by hand, and therefore a validation obligation, named under [Sanitization responsibilities](#sanitization-responsibilities).

### **Waiting for one response inside the request**

The generative endpoint answers synchronously as well: without `async` it runs the model call inside the request and returns the insight. That is one round trip, no operation IDs, no loop, no `setTimeout` in a control, and no deadline to choose.

We wait in the browser. A synchronous call puts a cold generation inside a PHP request that also has to survive `max_execution_time` — 30 seconds on a good deal of shared hosting — and a request cut short is indistinguishable from a service failure. Waiting in the browser is free and cancellable, and it reduces every plugin request to something that completes in well under a second. **The endpoint supports both, so this is a choice rather than a constraint** — and the deciding factor is that a timed-out synchronous call has spent a rate-limit token and thrown the generation away, where giving up on a wait leaves the generation running and collectable on the next mount.

### **A `GET` submit carrying the payload in the query string** {#a-get-submit-carrying-the-payload-in-the-query-string}

The submit could be a read, with the payload as a query parameter. It changes nothing on the site and returns generated text, so `GET:` describes it; a resolver reading a value is the shape the slice wants rather than an action writing one; both calls would sit on one branch of one route; and the `READABLE` branch's default is already wide enough for a view-only user, so a datapoint that forgot to narrow it would be a correctness nit rather than a feature that quietly stops working.

We `POST` the payload, because a query string puts a hard ceiling over the payload's row caps: roughly 8 KB of request line on a default Apache or nginx, and lower behind some WAFs. The failure mode is the worst kind — a 400 or 414 that depends on the host, appears only on sites whose page titles and search queries run long, and does not reproduce locally — and **a transport limit deciding how much a model gets to reason over is the wrong thing deciding it**. Encoding does not remove that ceiling; it postpones the measurement that finds it.

What the `POST` costs is two things, each named where it is implemented: a permission check that is now mandatory rather than merely correct, since the `EDITABLE` branch defaults to `MANAGE_OPTIONS` — see [Insight datapoints](#insight-datapoints); and one constraint on a cache key, since `API.set()` invalidates the datapoint's whole group by prefix and the slice's own entry has to live outside it — see [Browser-side caching](#browser-side-caching).

### **Re-submitting the payload instead of reading the operation**

The submit is idempotent from the plugin's side: the service consults its response cache before anything else, and the background worker writes its result into that cache, so re-sending the same payload returns the insight as soon as generation has finished. Waiting that way needs no second datapoint, no operation ID to hold, and no `GET` through the proxy — which would leave `Google_Proxy::request()` untouched.

We read the operation. **A re-submit that lands while the generation is still running misses the cache, spends a rate-limit token and starts a second generation of the same payload** — so the interval would be a cost rather than a latency choice, and a tight one against a slow model would spend most of a site's burst on a single narration. Reading the operation costs a memcache lookup outside the limiter, which makes the interval free to be short. It also makes a failed generation visible: a re-submit sees a cache miss and tries again, where a read sees `FAILED` and the widget reaches its Reporting state at once instead of waiting out the deadline.

The price is the read itself: a second datapoint, a handle the slice has to keep and expire, a 404 path, and the method argument on `Google_Proxy::request()`. That is a contained amount of machinery for removing the only call in the design that could duplicate a model run.

### **Keeping the submit and the read on one datapoint** {#keeping-the-submit-and-the-read-on-one-datapoint}

One `GET:benchmarking-insight` taking **either** a payload **or** an operation ID would put both service calls behind one name. Everything around the two branches is identical — the `OAuth_Client` resolution and therefore the shared-request behavior, the permission check, the shareability, the `WP_Error` mapping — so one datapoint means writing all of that once, and the store asks one question, "is the insight ready?", which one fetch store, one error key and one in-flight flag would answer.

We split them, and the split is not independently decidable: the payload has to travel in a body, a body means a `POST`, and `GET:benchmarking-insight` and `POST:benchmarking-insight` are two definitions by construction. **So this alternative is only available alongside [a `GET` submit](#a-get-submit-carrying-the-payload-in-the-query-string), and falls with it.** What survives on its own merits is what the split removes — a datapoint whose two parameters are alternatives, a contract that had to be stated as exactly-one-or-a-400 in its own validation rather than expressed in the definition array — and that what genuinely has to be shared is small enough for an abstract base to hold.

### **Keying the insight cache on the payload instead of the date range** {#keying-the-insight-cache-on-the-payload-instead-of-the-date-range}

The payload is the exact idempotency key for a generation: the same payload means the same insight, and keying the slice's cache entry on it would mean **the narration and the figures it describes always expire together**, so the widget could never show an insight about numbers it is not displaying. It is also the simpler thing to implement, since the payload is right there on the data response.

We key on the date range and the locale. The payload turns over whenever the figures move, which is hourly, and a changed payload is a fresh key on the *service's* cache as well — so payload keying does not merely miss the browser entry, it forces a new generation every hour for every date range anyone opens. That is a real model call and a real token, against a refill of two an hour, in exchange for a consistency nobody has asked for.

The consequence to be honest about is that a stored insight can be up to a day older than the figures beside it, with no threshold treating a large movement as a miss.

### **Returning the expected baseline from the service instead of computing it in the plugin** {#returning-the-expected-baseline-from-the-service-instead-of-computing-it-in-the-plugin}

The alternative was to grow the response schema with a per-day expected series, so the model lived in one place, next to the reasoning that narrates it, and the plugin only plotted what it was handed.

We compute it in the plugin. The [baseline](#expected-baseline) is arithmetic — nine weekly totals, seven weekday ratios, one year-ago ratio per day and a square root — over a report the datapoint runs anyway; there is no state to keep and nothing to train. Computing it in the datapoint also keeps the chart independent of the generative call, which matters more than anything else here: the bounds are derived before the proxy request and returned whether it succeeds, so a 429 or a timeout cannot take away the band the tab is built around. The service would otherwise need either a multi-year daily series in every request payload or the ability to query GA4 itself, and it does neither — it takes the finished `baseline` object from the plugin and narrates against it.

Within the model, longer moving-average windows, a stacked forward-trend multiplier, server-side Prophet or ARIMA, and a normalized-residual MAD band were each rejected: a simple moving average cuts weeks off at a hard boundary and, stretched far enough to be stable, drags out-of-season data into the level; a trend multiplier on top of an EWMA level double-counts growth; per-tenant time-series models need stored history and batch inference for accuracy that does not change what a site owner is told; and the MAD band needs residual sorting and medians to reach the same \~95% coverage the square-root ribbon gets in one expression. The [derivation of each constant](http://./expected-baseline-range-implementation.md) carries the detail.

### **Narrating the look-ahead in the service rather than in the plugin** {#narrating-the-look-ahead-in-the-service-rather-than-in-the-plugin}

`LOOKAHEAD` and `CURRENT_PERIOD_FORECAST` are already in the service's scenario set, and a generated sentence is a better sentence than a template: given the forward totals in the request payload, the model could name the weeks that are rising, tie them to what the site did last year and say it in the user's language, leaving the tab with one narrative voice instead of a generated paragraph followed by a plugin-formatted line.

We narrate it in the plugin. The request has no forward-looking field, so this is a service change before it is a plugin one, and it would gate an otherwise finished feature on another team's schedule — the same reasoning that [keeps the baseline in the plugin](#returning-the-expected-baseline-from-the-service-instead-of-computing-it-in-the-plugin), one step further along. The line is also the weakest case for generation there is: a direction and two numbers, which `sprintf` over a translated pattern renders exactly and Site Kit already translates, where generating it would attach a figure the plugin computed locally to the rate-limited, timed-out and failed paths of the [Reporting state](#insight-states).

If the request later grows a forecast field, the line becomes the service's to narrate and `LookaheadSummary` gives way to the insight block. Nothing else moves: the projection is already computed in the place it would be sent from.

### **Drawing the expected band across the whole thirteen-month window**

The band could cover every day the Typical Traffic chart plots rather than the selected range alone, which is the more complete-looking chart and needs no explanation of why the shading stops.

We bound it to the selected range. Judging the earliest day of a thirteen-month window at the full tier means reading more than two years back — history the 13-month gate does not guarantee and a report several hundred rows longer than the one the datapoint runs — and the band would be drawn at a degraded tier over exactly the months where the reader has the least ability to notice. It would also say nothing: a band around a year of past traffic is a statement about days nobody is asking about. The chart already shades the selected range, so the band lands where the reader is looking and the thirteen-month line stays what it is — context.

### **Skipping generation on a shared request** {#skipping-generation-on-a-shared-request}

The narrower reading of dashboard sharing is that the generative call belongs to the user who owns the token: a shared request serves the data and no insight, landing the view-only dashboard in the [Reporting state](#insight-states) the widget already implements. It needs no new decision about whose identity leaves the site, no view-only user can cause a generation, and the owner's rate-limit budget is untouchable by however many people the dashboard is shared with.

We generate on a shared request, under the owner's token. Every shared report in Site Kit already leaves the site under the owner's credentials, and the insight is derived from the same figures those reports return, so treating the narration differently from the numbers it narrates draws a line the data does not support. The cost of the narrow reading is the feature: the interpretation *is* what this epic adds, and a view-only dashboard showing two tabs of charts with the explanation removed is the state this epic exists to replace. What makes the exposure tolerable rather than unbounded is the service's 24-hour cache, which means repeat viewers spend tokens without causing generations.

## **Future Work**

### **A look-ahead the model cannot currently support**

The [look-ahead](#the-28-day-look-ahead) runs at the full baseline tier only. What is left for later is the two cases beneath it: a property with 9 weeks of history but no year-ago window, which can carry a weekday-shaped projection but nothing seasonal, and the horizon beyond four weeks, where the frozen level stops being a defensible assumption about the site's operating scale. Both need a forward model that is not simply this one run further, so neither is a matter of raising a limit.

### **A generation shared across a site's admins** {#a-generation-shared-across-a-site's-admins}

The service's response cache keys on the hashed Google user alongside the site, the end date, the locale and the payload, so two admins of the same site each pay for their own generation of an identical insight. View-only readers are already covered — they all resolve to the module owner — but a site with three people who log in has three generations of the same narration about the same site.

Dropping the identity from that key would make a generation a site-level artifact, which is what it actually is: it is derived from aggregate figures and says nothing about who asked. It is not obviously free — the key is presumably per-user so that one user's request cannot serve another's data — but the payload is already in the key, and a payload is only assembled from data the requesting identity could read. Deciding that belongs with the service rather than here, which is why it is future work; the plugin needs no change either way.

### **Low-traffic language**

Low-traffic sites need more cautious language and de-emphasized short-term change. The band handles its share of that already: it averages whole weeks rather than days, and its square-root scaling plus its floor of 5 visitors give a 25-visitor-a-day site a ±60% band, so ordinary noise stays inside expectations. What remains is the narration's language, which is generated service-side from the same payload and belongs in the service's prompt and heuristics.

### **Richer feedback than thumbs up / down**

Letting users say *why* an insight was not relevant — wrong data, out of date, not useful — is a stretch goal beyond the thumbs signal. The service's survey to downvoters is the hook for it; a structured in-product form is future work.

## **Dependencies** {#dependencies}

The epic depends on the Site Kit Service's `/v1/ai/benchmarking` endpoint, which in turn depends on the Agent Platform API. It is implemented — request and response schemas, scenario set, rate limiting, caching, output validation and asynchronous execution — and **its availability in production is the gating dependency** for every insight-rendering issue. The baseline model, the two charts' bands and overlays, the content markers and the look-ahead can all be built and tested against fixtures before then, and none of them calls the service at all.

**The design rests on two properties of that endpoint's pipeline, and every economic claim in this document rests on the first:**

1. **The response cache is consulted before the rate limiter.** A submit the service can answer from its 24-hour cache costs neither a token nor a model call. That is what makes a shared dashboard cost what one reader costs, what makes a give-up on [the wait](#waiting-for-the-generation) recoverable for free on the next mount, and what takes the pressure off the plugin's own cache lifetimes. **Reversing the two would change what this feature costs a site rather than only how fast it is**, which is worth knowing before anyone reorders that pipeline again.  
2. **An asynchronous submit can answer synchronously.** On a cache hit the service returns the insight and no operation ID, which is the same mechanism read from the other side: the background worker writes its result into that cache, so the submit that follows a finished generation returns it.

Two properties of the operations endpoint the [wait](#waiting-for-the-generation) relies on: **it has no rate limiter in front of it**, so reading an operation is free however often it happens; and **an operation lives for one hour**, refreshed when its worker finishes, past which a read is a 404 the slice treats as a stale handle. It authorizes the caller as a registered user of the site rather than as the user who started the operation, which is what a shared request depends on and is discussed under [Security](#security).

Its schema is still moving, which is the risk worth naming: the response gained `top_dimensions`, `driver` and `actionable_recommendation`, the request gained `baseline` and three new `contextual_data` dimensions, and the endpoint gained asynchronous execution — all in the weeks either side of this design. The plugin should treat the response as additive — render what it recognizes, ignore what it does not — rather than validating the payload shape strictly and failing on a field it has never seen.

When the service is unavailable, the widget renders everything except the insight, and structurally rather than by careful ordering: the numbers arrive in a response no proxy call sits in front of. GA4 outages are what take the widget down, and they surface through the existing error path; a Search Console outage costs one factor section.

## **Migrations**

No migrations are required. Nothing this epic adds is persisted anywhere in the plugin: the baseline, the payload and the insight are all assembled and discarded inside the requests that produce them, and the only copies are in the requesting user's own browser storage.

## **Technical debt**

Three items:

1. **One cache key has to stay outside a group the API layer clears.** `API.set()` invalidates `modules::analytics-4::benchmarking-insight` and everything prefixed by it after each submit, so the slice's own insight entry is stored under a group of its own. Nothing enforces that: a later rename that brings the two names back into alignment would turn every submit into a silent eviction of every other date range's insight, and the symptom is a cache that merely looks cold. The mitigation is a test that asserts a submit for one range leaves another range's entry in place — see [Browser-side caching](#browser-side-caching).  
2. **The plugin derives analytical inputs for a model it does not own.** If the service later grows the ability to query GA4 itself, `Expected_Baseline` and the payload builder become dead weight and should be removed rather than maintained. Keeping them in classes with no REST or widget dependencies is what makes that removal a deletion rather than an unpicking.  
3. **The payload's fields appear twice in one response.** Once positionally for the widget to draw and once encoded for it to forward. It is a couple of kilobytes and it is deliberate — see [Wire format additions](#wire-format-additions) — but it is a duplication a later reader will want explained, and the explanation has to survive in the encoder's own comments and not only here.

# **Quality attributes**

## **Security** {#security}

The new surface is two outbound proxy calls carrying the site's own analytics data, and one REST parameter that carries a payload back out of the browser. Four risks:

**Prompt injection.** Page titles and search queries in `contextual_data` originate from site content and from visitors' searches. The service separates system instructions from data, delimits the data, strips markup and truncates titles. The plugin contributes by sending structured fields only — there is no free-text field in the payload — and by validating what the public contextual-data filter hands back before it reaches the payload at all.

**A payload that arrives from the browser.** This is the one genuinely new risk. `POST:benchmarking-insight` takes the payload in a request body, so what reaches the model is no longer bounded by what a PHP function assembled in the same request — it is bounded by what an authenticated dashboard user chooses to send. The datapoint therefore validates the decoded structure as it would any request body, per [Sanitization responsibilities](#sanitization-responsibilities): known fields only, declared row shapes, scalars cast, string lengths and row counts held to the encoder's own caps. What an attacker gains even without that check is narrow — the narration comes back to them and to nobody else, and the service's delimiting and output validation stand behind it — but the check is what keeps the payload a structured document rather than a free-text channel into a prompt, and it is not optional for that reason.

**Rendered model output.** `text`, `driver` and `actionableRecommendation` are all model-generated and all rendered in the dashboard. Each is treated as untrusted text and rendered as a React child, never as HTML — the service's own narrative safety checks are a second line of defense, not a licence to trust the string. `scenario` and each `topDimensions` entry are validated against the known sets before they select a treatment.

**Authorization.** Both datapoints implement `Permission_Aware_Datapoint`, so the existing `REST_Modules_Controller` permission dispatch applies — and it applies on both branches of the route, resolving `"{$method}:{$datapoint}"` before deferring to the branch default. **The read and the submit declare the same capability for opposite reasons.** Left to the route, the read would inherit the `READABLE` branch's broader view-insights default, and the submit would inherit the `EDITABLE` branch's `MANAGE_OPTIONS` — too wide in one direction and too narrow in the other, the second of which fails closed and takes the narration out of every shared dashboard rather than leaking anything. The service independently verifies the bearer token, resolves it to a hashed Google user ID and requires that user to be registered against the requesting site, so a valid `site_id`/`site_secret` pair alone cannot buy an insight.

**An operation is scoped to the site, not to the user who started it.** The service's operations endpoint verifies that the caller is registered against the site and that the operation belongs to it, and does not tie an operation to the user who started it, so any registered user of a site could read any operation of that site given its ID. The IDs are `shortuuid`s, are never rendered, and live an hour; what an operation holds is this site's aggregate Analytics data, which every reader of the widget already has access to by authentication or by sharing role.

**A view-only user's request generates under the owner's identity**, which is the same delegation every shared GA4 report already performs, under the same conditions and widened by nothing here. What the delegation can be spent on is bounded by the service's own cache, which its pipeline consults before its limiter, so a dashboard shared with fifty people costs the owner's budget what one reader costs.

## **Reliability**

The generated insight is best-effort and the widget is designed to survive its absence: a failed, rate-limited or abandoned generation leaves every metric, chart, band and factor in place. This is the single most important reliability property of the design, and it is worth stating plainly — **the numbers must never disappear because the narration failed.**

**Splitting the requests is what makes that structural rather than careful.** The numbers arrive in a response that never touches the proxy, so no failure of the generative endpoint, no timeout, and no rate limit can reach them; there is no ordering to get right and no field to remember to omit.

No plugin request in this design is long. The data request is reports and arithmetic, the submit returns as soon as the service has created an operation, and every read after it is the same short call — so `max_execution_time` and a reverse proxy in front of the site stop being things the design has to be budgeted against. **The one failure that still takes the widget down is a GA4 failure**, which is what the existing error path exists for.

**The wait's two failure modes both recover by doing something the design already does.** A deadline that passes leaves the generation running service-side and its result landing in the service's 24-hour cache, so a later mount collects it in one submit the cache answers. A handle that has expired answers 404, and the slice submits again. Neither loses work, and neither costs more than the request it takes to find out.

Ordinary navigation does not consume the rate-limit budget. A repeat load over the same dates in the same tab is answered from the [browser cache](#browser-side-caching) without reaching the server; a new tab, a new login session or a new reader reaches the server but is answered from the service's cache without spending a token. What costs a token is a payload the service has not generated for. What the widget shows once the budget is gone is an [open question](#what-does-the-widget-show-when-the-rate-limit-is-hit?).

## **Privacy**

The payload sends the site's own analytics data to the Site Kit Service: aggregate visitor counts, top URLs with their titles and publication ages, channel names, content category names, search queries with their clicks and positions, and the period-scale baseline summary. All of it is data the requesting user can already read in the dashboard, and all of it is derived from reports the plugin already runs.

No personally identifiable information is sent. Search queries are Search Console's already aggregated and anonymized queries. Raw requests and model responses are not logged persistently by the service; the only retained telemetry is the privacy-safe thumbs signal — the vote ID, feedback direction and locale.

No new OAuth scope is requested. No new custom dimension is created, so no additional data is collected from site visitors.

**Nothing this feature reads or sends is stored on the site.** Both responses and the payload are assembled and discarded inside the PHP request that produced them. Where they are held is browser storage, under a key prefix hashing the WordPress user's login, their auth session token and the blog ID — so an entry does not outlive a logout and does not reach another user of the same browser. That applies to the insight's entry as much as to the data's: the slice writes it through the same storage helpers the API layer uses, so it inherits the same scoping rather than inventing its own.

## **Scalability**

**The unit that scales is the number of distinct date ranges opened per identity per day.** A full miss costs two batched GA4 calls, one Search Console call, one rate-limit token and one generation. A repeat over the same dates in the same tab is answered from the [browser cache](#browser-side-caching) without reaching the server, and a repeat from anywhere else is answered from the service's cache without spending a token. **A shared dashboard's cost does not grow with its viewers**, since they all resolve to the module owner's identity; what does add a unit is a second admin, because the service's cache key carries the identity — see [Future Work](#a-generation-shared-across-a-site's-admins).

Report count grows by one in this epic — the two-years-back totals — and stays at two batched calls. The daily series grows from 395 rows to `days_in_period + 392`, so 482 at the widest range the selector offers, well inside the GA4 Data API's default row limit. The arithmetic over it is linear in the number of days, and the response grows only by the bounds on the days inside the selected range, the 28 forecast rows and the content markers.

The payload is the one thing that leaves the site, and its size is bounded by the row caps rather than by the site: a couple of kilobytes, base64url encoded, whether the site has ten posts or a hundred thousand.

Waiting adds requests but not load worth counting: each is one proxy call answered from a memcache lookup, outside the rate limiter and starting no work. The submit happens once per generation.

## **Accessibility (a11y)**

**The insight arrives seconds after the rest of the panel, so its container is a polite live region.** Text appearing in a card a screen-reader user has already begun reading is otherwise silent, and the block reserving its space while [the generation is waited on](#waiting-for-the-generation) is what stops the arrival moving the content under them. **Neither the arrival nor the factor section's re-sort may move focus.**

The insight block needs adequate contrast for the scenario-driven emphasis, and the fact that the text is AI-generated has to be conveyed in text rather than by an icon alone.

Both charts gain information that must be reachable without sight of the plot and without hovering: the expected range's bounds per day, the boundary between the measured and projected halves, and the content markers — whose tooltips carry post titles and links that must be reachable by keyboard, not only by clicking a four-pixel dot. `LookaheadSummary` is already text, which is the easy case and the model for the rest.

## **Internationalization (i18n)**

Plugin-side strings are translated as usual, including every word of `LookaheadSummary` and the `HISTORICAL_BASELINE` factor's copy, which are `sprintf` over translated patterns rather than concatenated fragments.

The generated insight is a different matter: `text`, `driver` and `actionableRecommendation` are produced by the model, in the language the plugin declares through the `hl` parameter, and none of them passes through the WordPress translation pipeline. Where plugin chrome wraps generated text, the two must not be concatenated into one translatable string. Numbers inside the insight are formatted by the service rather than by `numFmt()`, which is a consistency risk worth watching during QA — `driver` in particular exists to quote figures, so a locale where the model's number formatting diverges from `numFmt()` will show two conventions in one card.

The character limits the service works to — 250, 210 and 400 — are counted on the localized string, and languages that expand under translation will sit at the top of that range. Both the teaser and the full block have to absorb the longest case rather than being tuned to the English one, and the teaser is the tighter of the two because it is one line in a card that has a chart under it.

# **Project management**

## **Work estimates**

| \# | Title | Design Doc Points | GH Points |
| :---- | :---- | :---: | :---: |
| 1 | Traffic Insights feature flag | 3 |  |
| 2 | Add the two generative proxy methods and the `GET` capability to `Google_Proxy` | 11 |  |
| 3 | Add the `POST:benchmarking-insight` and `GET:benchmarking-insight` datapoints over their shared base | 15 |  |
| 4 | Make both insight datapoints shareable | 7 |  |
| 5 | Compute the expected baseline range from the GA4 daily series in PHP | 19 |  |
| 6 | Extend the baseline model with the 28-day look-ahead projection and its comparison | 11 |  |
| 7 | Assemble the benchmarking request payload in PHP and extend the wire format to carry it | 15 |  |
| 8 | Derive the content markers in `GET:benchmarking-data` | 7 |  |
| 9 | Extend the `benchmarking` slice with the insight resolver, its wait loop and its own cache entry | 19 |  |
| 10 | Traffic Overview: generated insight teaser | 11 |  |
| 11 | Typical Traffic: generated insight block | 11 |  |
| 12 | Add the "Is this helpful?" prompt beneath the insight | 3 |  |
| 13 | Traffic Overview: content markers on the traffic chart | 15 |  |
| 14 | Typical Traffic: expected baseline band on the chart | 15 |  |
| 15 | Typical Traffic: look-ahead forecast and its projection line | 15 |  |
| 16 | Insight-driven factor ordering and the historical-baseline factor | 11 |  |
| 17 | Generating, unavailable-insight and error states | 11 |  |
| 18 | Add support links for the insight and the expected baseline | 7 |  |

**TOTAL: 206 STORY POINTS across 18 issues**

**The proxy work is separated from the datapoints deliberately.** `Google_Proxy` first, because the `GET` capability is a change to a Core class every proxy call in the plugin passes through and deserves reviewing as one rather than inside a feature issue; then the two datapoints over it, which is where the payload validation, the `WP_Error` statuses and the permission check each branch of the route needs live. Neither touches a report, so both are testable against a mocked proxy alone.

Sharing is critical path rather than a follow-up: without it the view-only dashboard shows every number and no narration. It is separate from the datapoints because it is verifiable on its own — a dispatch as a view-only user against a mocked proxy — and because the submit's permission test is the assertion that fails if the datapoint ever stops declaring its own check.

The baseline is the model itself — the three factors, the band, the fallback tiers and the [`baseline` payload object](#what-the-service-is-told), as PHP with its own fixtures — where the chart issue is only the columns and the `intervals` option that plot the bounds the response carries. They are split because the model is the analytically substantial part and is verifiable on its own. The model feeds payload assembly as well, so it lands before that is finished rather than after it.

The [look-ahead](#the-28-day-look-ahead) splits along the same line and lands after both halves of the baseline: the forward loop, the widening band, the tier rule and the boom-or-lull comparison as PHP, then the chart's forward columns, the joining row, the dashed series, the two overlays and `LookaheadSummary` on top of a chart that already renders. Building the chart once with both halves would be marginally cheaper than building it twice, and is not worth holding the measured band — the part the tab is named for — behind a projection only the full tier can produce.

**The datastore work is the epic's biggest single risk of an invisible mistake.** It owns two fetch stores, the resolver that chains the insight onto the data, its own cache entry with three kinds of value and two lifetimes, the handle it holds between ticks, the control that schedules the next read, the deadline, the 404-and-resubmit path, and the mapping from either fetch store's status onto the insight states. Every one of those is wrong-but-plausible in a way that looks identical on screen: a cache entry that never hits costs a generation per mount, a pending marker that is not written costs a second generation on every reload, an entry keyed on the payload quietly reverts the day-long lifetime, and an entry stored inside the datapoint's own cache group is evicted by the next range's submit. **Its acceptance criteria have to include request counts and cache reads, not only the rendered result** — which is what puts it at 19\.

**The two chart issues are sized against the [PoC](#chart-construction), not against a plain `LineChart`.** The content-marker issue lands first among them and therefore builds what both charts stand on — `ChartLegend`, the tooltip card and the marker click handling — on top of its own dot series. The baseline and forecast issues' own risk is the overlays: they are positioned from the chart layout interface in a `ready` callback, and the stacking-context rule that makes the shaded region visible is the kind of thing that is invisible in review and obvious in VRT.

The factor-ordering issue is small in code and awkward in behavior: it owns the `DimensionType` map, the `HISTORICAL_BASELINE` entry, and the rule that the section renders before `topDimensions` exists and re-sorts when it arrives without moving focus. That last part is what its brief has to name.

The states issue lands late because it needs every other section present to be worth reviewing, and it owns the two absent states of the insight block — the placeholder while a generation is coming, and nothing at all once it is not.

The server-side half is the critical path in full. Every band, marker and overlay can be built and tested against a fixture before the service endpoints are available in production; the two insight sections, the factor ordering and the unavailable-insight states are the ones whose acceptance needs the live endpoints, since `topDimensions` and the scenario codes drive what they order and render, and since only a real generation shows how long the **Generating** state actually lasts.

## **Documentation in-product** {#documentation-in-product}

The widget needs support links in two places, resolved through `getDocumentationLinkURL()` on `core/site`:

1. **A "Learn more" link on the insight**, explaining what it is, how it is generated, and that it is AI-generated — the last part is not optional, and it belongs on both the teaser and the full block.  
2. **A link explaining the expected baseline**, sitting beside the chart's legend and repeated in the hover card over its [projected region](#expected-baseline-on-the-typical-traffic-chart). It covers the least self-evident thing in the widget: that the range is derived from the site's own history and not from other sites, that it is wider on a small site because a small site's traffic genuinely varies more, that a day inside the band is a normal day rather than a good one, that the shading covers the selected period and not the whole window, and that the dashed half has not happened yet.

The support team drafts these before rollout; the slugs are added with the links.

## **Testing plan considerations**

The hard part is data. A meaningful insight needs a property with a real seasonal shape, and the scenario mix depends on figures that fall above, inside and below the expected range. QA therefore depends on tester-plugin support for forcing both responses and the failure modes, listed under [Tester plugin](#tester-plugin), plus access to an Analytics property with genuine history.

**The [baseline](#expected-baseline) is the one part of the feature whose numbers can be asserted exactly.** Its deterministic seeding and clamping rules mean a fixture of daily rows has one correct answer per day, so PHPUnit should cover the model on hand-built series: a clean nine-week series, a viral week that must be clamped at `2x`, a tracking outage that must be clamped at `0.5x`, days GA4 omitted entirely, a year-ago window that ends mid-range, and each of the three history tiers.

The [look-ahead](#the-28-day-look-ahead) is asserted the same way and adds four cases: a forward day whose seasonal multiplier is known by construction, the band's `1.05x` to `1.20x` widening across the four weeks, each side of the ±15% boom-or-lull threshold, and a weekday-only tier that must return no `forecast` at all.

PHPUnit also covers both insight datapoints: the payload validation each rejects a hand-written request with, and the mapping from a proxy status or a `FAILED` operation onto a `WP_Error`. **The submit needs a permission test of its own** — a view-only user must reach `POST:benchmarking-insight`, which is the assertion that fails if the datapoint ever stops declaring its own check and falls back to the `EDITABLE` branch's `MANAGE_OPTIONS`. The view-only path belongs here rather than only in manual QA: a shared request must resolve the owner's client for the submit and for the operation read, and the disconnected-owner case belongs beside it — the submit must not be attempted where no token resolves.

**The wait loop and the slice's cache entry need tests that count requests and cache reads, not just states.** Every mistake available here renders correctly and costs tokens or generations, so Jest should assert: **one** submit and N operation reads for a generation that reports running N−1 times; **no request at all** when the slice's entry holds an insight; no request when it holds a 429 marker, and one once that marker has expired; a read rather than a submit when the entry holds a pending operation ID; exactly one fresh submit when a read answers 404; a single submit when both panels mount in sequence; no further read after the deadline; an entry written under the date range and locale rather than under the payload — which is the one that silently reverts the day-long lifetime if it regresses; and **a submit for one date range leaving another range's stored insight in place**, which is what `API.set()`'s prefix invalidation breaks if the slice's own key ever moves inside the datapoint's group.

**The chart tables are unit-testable and the charts themselves are not.** The [column order](#chart-data-tables-and-options) the interval roles depend on, the joining row, the null placement each half relies on, the bounds' confinement to the selected range and the axis maximum computed over the bands are all outputs of pure functions, and each has an exact expected value. What is left — the band rendering as an area, the dash on the projected line, the divider landing on the split, the shaded region painting behind the line rather than behind the card, the marker dots' white stroke — is VRT's, which is why both charts carry Storybook stories at every state the response can put them in. Storybook should carry the Typical Traffic chart at small, medium and large traffic so the band's proportional behavior is in VRT, along with the weekday-only and same-weekday-average tiers.

**The wire format is the one thing both suites test, and they test it against the same file.** The shared fixture gains the new members and one case beyond them: a payload that survives a PHP encode, a JSON round trip and a PHP decode byte for byte — the property both cache keys depend on. The version integer increments in this epic, so a fixture at the previous version must be rejected rather than guessed at.

Three response-driven cases are worth naming because they are easy to miss and awkward to reach naturally: a `topDimensions` list naming a dimension whose `contextualData` key is absent; `[ HISTORICAL_BASELINE ]` on its own, which is what the service substitutes when the model names nothing valid; and two posts sharing one publish date, which must produce one marker naming both.

One case the design removes is worth noting for what QA no longer has to construct: no request in this design is long enough for `max_execution_time` to fire inside it, so there is no host-timeout scenario. What replaces it is the payload's validation — a hand-written request to `POST:benchmarking-insight` carrying an over-long string, too many rows, or a field of the wrong type must be rejected rather than forwarded, and that belongs in PHPUnit rather than in QA.

## **Launch plans**

The epic follows Site Kit's usual staged rollout. The `trafficInsights` flag is enabled for 20% of users through the Site Kit Service, and — absent critical issues — for the remaining 80% two weeks later.

**The rollout cannot start before the service's `/v1/ai/benchmarking` endpoint is live in production**, since the insight is the defining half of what this epic adds. The plugin work can complete and ship behind the disabled flag ahead of that, and the baseline, the forecast and the content markers are all testable without it.

Two things to watch in the first cohort rather than after it: how long the **Generating** state actually lasts, which is what settles [the cadence question](#what-is-the-cadence-and-deadline-for-waiting?), and how often a 429 is reached, which is what settles [the rate-limit question](#what-does-the-widget-show-when-the-rate-limit-is-hit?). Both are measurable at 20% and neither is answerable before then.

An issue to remove the `trafficInsights` flag and its conditional logic should be raised once the feature is stable at 100%.

# **Open questions**

## **What is the cadence and deadline for waiting?** {#what-is-the-cadence-and-deadline-for-waiting?}

[Waiting](#waiting-for-the-generation) needs three numbers: how long to wait before the first operation read, how long between reads after that, and when to stop. **Reading an operation costs a memcache lookup outside the rate limiter and starts no work**, so this is a latency and chatter question rather than a cost one — which is what reading the operation buys over re-submitting the payload, and why the interval can be short.

What decides it is how long generation actually takes, which is measurable as soon as the endpoint is live. The deadline is the part with the real trade-off, and it is mild in both directions: giving up early is cheap, because the generation finishes regardless and the next mount collects it from the service's cache; giving up late leaves a placeholder on screen long enough to read as broken.

Undecided: the three values; whether the interval widens as the wait goes on, which is the obvious hedge against a long tail and mostly a question of how much request chatter is acceptable in a dashboard tab someone leaves open; and whether a give-up should say anything different from a failure, given that the insight may well appear on the next load.

Blocked on this: nothing structural — the loop is the same loop at any cadence.

## **How is the `baseline` payload object derived?** {#how-is-the-baseline-payload-object-derived?}

The chart needs a daily range; the payload needs six fields on a period scale, and three of them have no definition in the daily model:

1. `expected_range_min` and `expected_range_max` are bounds on the period total. Summing the daily bands is wrong — variance adds while the band scales with a square root, so the summed band is several times too wide and would report almost everything as `WITHIN_EXPECTED`. Recomputing `3 * SQRT` on the summed prediction is the obvious alternative, but the multiplier and floor that were tuned for daily counts have not been checked at period scale.  
2. `trend_direction` needs a rule over the `weekly_ewma` series and a dead band for `STABLE`. The service uses ±5% for its own trend calls, which is a reasonable precedent, not a decision.  
3. `is_seasonal_period` needs a threshold on how far `annual_seasonality` has to move the expectation before the period counts as seasonal. This one has teeth: the service refuses to use the word "seasonal" in any language unless this flag is true, so setting it too conservatively removes the feature's most distinctive explanation, and too loosely puts "seasonal" on ordinary noise.

Also undecided: whether the short-history tiers send a `baseline` with `period_months: 2` or omit the object and let the service narrate against the previous period instead.

Blocked on this: the payload half of the baseline model, and the scenario mix QA will actually see.

## **What does the widget show when the rate limit is hit?** {#what-does-the-widget-show-when-the-rate-limit-is-hit?}

The service allows a burst of 10 with a refill of 2 per hour per site and user, and returns 429 beyond that. With the cache consulted ahead of the limiter, a token is spent only on a payload the service has not generated for, so reaching the limit takes a run of genuinely new date ranges — or a [wait cadence](#what-is-the-cadence-and-deadline-for-waiting?) tight enough to spend the burst on duplicate submits, which is the failure this question is most likely to arrive by.

The numbers stay on screen either way. What is open is the insight block: rendering the tab without it, or saying in its place that the insight is temporarily unavailable and will be back shortly, are materially different experiences, and the choice decides whether a 429 is tracked as an error.

The mechanics are settled: a 429 is a `WP_Error`, the [slice](#datastore-slice) holds it as a marker under the date range for an hour — the time the limiter takes to refill two tokens — and the next mount is answered from that marker rather than re-submitting. The service sends no `Retry-After` header, so the hour is the plugin's own figure. What is left to decide is the copy, whether a 429 is tracked as an error at all, and whether the retry after the hour happens silently on the next mount or only when the user asks for it.

## **Does the baseline's trailing window include the days it judges?** {#does-the-baseline's-trailing-window-include-the-days-it-judges?}

The [baseline](#expected-baseline) measures `weekday_factor` and `daily_base_level` over the 63 days ending on the selected range's `endDate`, which for the default 28-day range are largely the same days the band judges. A month that is genuinely down therefore lowers its own expectation, and the band tracks the actuals more closely than a true out-of-sample expectation would.

Ending the trailing window where the selected range begins removes the overlap, at the cost of a level anchored a full range back — staler, and lagging a genuine structural change by the length of the selected range — and of a higher bar for the short-history tiers, which would then need 63 days *before* the earliest judged day rather than 63 days in total. The report span is unaffected either way: the seasonality lookback already reaches deeper than both windows.

Blocked on this: the window arithmetic in the baseline model, and the history each fallback tier requires.

## **What are the minimum data thresholds for each level of analysis?** {#what-are-the-minimum-data-thresholds-for-each-level-of-analysis?}

How many months and how much daily volume are needed before anomaly, seasonality and trend analysis are trustworthy is unresolved. The plugin gates on GA4 property age alone. Whether the gate belongs in the plugin or in the service — which could return a "not enough data" scenario — changes where the logic lives.

The [baseline](#expected-baseline) has thresholds of its own and they do not line up with the gate. Its full tier needs 392 days of history before the earliest day it judges — 420 days at the default range, 482 at the 90-day range — where the gate asks for 13 months; its weekday-only tier needs 9 weeks; and below that it falls back to a same-weekday average. The band is drawn at whichever tier the history supports rather than hidden, so what is open is the floor rather than the fallbacks. Volume is a second axis the gate does not look at at all: the band's floor of 5 visitors keeps a very small site's expectations honest, but nothing currently declines to draw a baseline for a site averaging two visitors a day, and nothing declines to ask a model to explain a movement of four.

Blocked on this: whether the baseline and the insight need a floor of their own beneath the history gate, and whether a site can be too small for either regardless of how long it has been collecting.

# **Appendices**

The following are implementation-level details that can be settled at the Implementation Brief stage of the individual issues.

### **Datapoint response shapes**

What the response gains once decoded, and therefore the fixtures every front-end issue is built against. These are the plugin's own shapes: camelCase, flat, and unrelated to the service contract below except for the five generated fields.

Added to `GET:benchmarking-data`:

| Field | Type | Present |
| :---- | :---- | :---: |
| `dailyTraffic[].expectedMin`, `expectedMax` | `integer`, or `null` outside the selected range | Always |
| `forecast` | `Array<{ date, predicted, expectedMin, expectedMax }>`, 28 rows from the day after `endDate` | At the full baseline tier |
| `lookahead.direction` | `BOOM` | `LULL` | `STEADY` | At the full baseline tier |
| `lookahead.projectedVisitors` | `integer`, the 28 forward predictions summed | At the full baseline tier |
| `lookahead.changePercent` | `float`, signed, against the 28 days of actuals ending on `endDate` | At the full baseline tier |
| `baseline.periodMonths` | `integer` | Always |
| `baseline.expectedRangeMin`, `baseline.expectedRangeMax` | `integer`, period totals | Always |
| `baseline.trendDirection` | `UP` | `DOWN` | `STABLE` | Always |
| `baseline.statusVsBaseline` | `ABOVE_EXPECTED` | `WITHIN_EXPECTED` | `BELOW_EXPECTED` | Always |
| `baseline.isSeasonalPeriod` | `boolean` | Always |
| `baseline.tier` | `full` | `weekday` | `weekday_average` | Always |
| `contentMarkers` | `Array<{ date, visitors: int, posts: Array<{ title, url }> }>` | With the post-date custom dimension |
| `payload` | `string`, base64url, opaque to the browser | Always |
| `locale` | `string`, resolved server-side | Always |

`POST:benchmarking-insight`, given a `payload` in the request body's `data` object, which answers with one of the two groups and never with both:

| Field | Type | Present |
| :---- | :---- | :---: |
| `operationID` | `string`, the handle the slice polls with | When the service started generating |
| `insight.scenario` | `string`, one of the eleven codes | When the service answered from its own cache |
| `insight.topDimensions` | `Array<DimensionType>`, up to 3, ranked | When the service answered from its own cache |
| `insight.text`, `insight.driver`, `insight.actionableRecommendation` | `string`, localized | When the service answered from its own cache |

`GET:benchmarking-insight`, given an `operationID`:

| Field | Type | Present |
| :---- | :---- | :---: |
| `done` | `boolean` | Always |
| `state` | `PENDING` | `RUNNING` | `SUCCEEDED` | `FAILED` | Always |
| `insight` | the five fields above | On `SUCCEEDED` |

A failure is a `WP_Error` rather than a field — a `FAILED` operation included, so the slice reads one thing rather than a status and an error side by side.

### **Wire format additions**

The members the [format](#wire-format-additions) gains, beside the ones it already carries. The index names live in `constants.ts` on the JS side and in class constants on the PHP side.

| Envelope member | Holds |
| :---- | :---- |
| existing | version, string table, first plotted date, daily series, period totals, contextual dimensions, dimension order |
| new | the per-day bounds, one row of `[ expectedMin, expectedMax ]` per plotted day, `null` outside the selected range |
| new | the forecast days, one row of `[ predicted, expectedMin, expectedMax ]` per day, or empty below the full tier |
| new | `[ direction, projectedVisitors, changePercent ]`, or empty with the forecast |
| new | the baseline: `[ periodMonths, expectedRangeMin, expectedRangeMax, trendDirection, statusVsBaseline, isSeasonalPeriod, tier ]` |
| new | the content markers, one row of `[ dayOffset, visitors, [ titleRef, urlRef ], … ]` per publish date |
| new | the resolved locale, as a string-table reference |
| new | the encoded payload, as one opaque string |

The payload is the same positional layout restricted to the string table, the baseline, the period totals, the contextual dimensions and the dimension order, plus `end_date`, `days_in_period` and the resolved locale, JSON-encoded and then base64url-encoded, optionally deflated first with a one-character prefix saying which.

### **Chart data tables and options** {#chart-data-tables-and-options}

The two data tables, in column order. Interval columns annotate the series column they follow, so the order is load-bearing rather than cosmetic. `null` in a cell is a gap the chart must not interpolate across.

| \# | Traffic Overview chart | Typical Traffic chart |
| :---- | :---- | :---- |
| 0 | `date` | `date` |
| 1 | `visitors` | `visitors` |
| 2 | marker value — the day's `visitors` on a publish date, else `null` | `expectedMin`, `role: 'interval'`; `null` outside the selected range |
| 3 | — | `expectedMax`, `role: 'interval'`; `null` outside the selected range |
| 4 | — | projected value — `null` until the last measured row, which repeats that day's `visitors` |
| 5 | — | forecast `expectedMin`, `role: 'interval'`; the last measured row repeats that day's `visitors` |
| 6 | — | forecast `expectedMax`, `role: 'interval'`; the last measured row repeats that day's `visitors` |

Columns 4 to 6 and their rows are absent below the full baseline tier. The marker column is absent when `contentMarkers` is empty.

The options that carry a decision rather than a preference:

| Option | Value | Why |
| :---- | :---- | :---- |
| `intervals` | `style: 'area'`, a fill color and opacity, `lineWidth: 0`, `pointSize: 0` | Draws each band as a filled region with no boundary lines or points. |
| `interpolateNulls` | `false` | Keeps the band inside the selected range and each of the two lines inside its own half. |
| `tooltip` | `trigger: 'none'` **with `isHtml` set explicitly** | `getChartOptions()` overrides `trigger` unless `isHtml` is already declared. |
| `focusTarget` | `'datum'` | Makes `select` resolve to the point that was clicked, which is what the marker tooltip keys on. |
| `legend` | `position: 'none'` | The legend is `ChartLegend`, since Google Charts cannot show a band in its own. |
| `series` for the projected line | `lineDashStyle` | The dash, the divider and the tint are what separate the halves; the color does not change. |
| `series` for the marker column | `lineWidth: 0` with a `pointSize` | Renders the column as dots on the line rather than as a line of its own. |
| `vAxis` viewWindow max | computed over the bands | An axis fitted to the lines clips the bands. |

### **Request and response field mapping**

The service contract, for reference while implementing the derivation. Every `contextual_data` key is optional, and each `MetricPair` is `{ current, previous }`:

| Request field | Type | Required |
| :---- | :---- | :---: |
| `end_date` | `string` (YYYY-MM-DD) | Yes |
| `days_in_period` | `integer` | Yes |
| `visitors` | `Array<MetricPair<int>>` — index 0 this year, 1 last year, 2 two years ago | Yes |
| `ranked_dimensions` | `Array<DimensionType>`, the plugin's own ordering, highest impact first | No |
| `async` | `boolean`; when true the service returns an operation instead of generating in the request | No |
| `baseline.period_months` | `integer`, must be positive | No |
| `baseline.expected_range_min`, `baseline.expected_range_max` | `integer`, period totals | No |
| `baseline.trend_direction` | `UP` | `DOWN` | `STABLE` | No |
| `baseline.status_vs_baseline` | `ABOVE_EXPECTED` | `WITHIN_EXPECTED` | `BELOW_EXPECTED` | No |
| `baseline.is_seasonal_period` | `boolean` | No |
| `contextual_data.recent_content_momentum` | `Array<{ url, title, published_days_ago, visitors }>` | No |
| `contextual_data.search_query_shifts` | `Array<{ query, url?, clicks: MetricPair<int>, position: MetricPair<float> }>` | No |
| `contextual_data.traffic_channel_surges` | `Array<{ channel, device?, url?, visitors: MetricPair<int> }>` | No |
| `contextual_data.category_resonance` | `Array<{ category_name, top_urls_count }>` | No |
| `contextual_data.user_mix_shifts` | `Array<{ segment, url?, visitors: MetricPair<int> }>` | No |
| `contextual_data.device_shifts` | `Array<{ device, url?, visitors: MetricPair<int> }>` | No |
| `contextual_data.referrers` | `Array<{ referrer, visitors: MetricPair<int> }>` | No |

| Response field | Type | Present |
| :---- | :---- | :---: |
| `operation_id` | `string` | With `async`, unless the response cache answered |
| `scenario` | `string`, one of the eleven codes below | On a generated or cached answer |
| `top_dimensions` | `Array<DimensionType>`, up to 3, ranked | On a generated or cached answer |
| `text` | `string`, localized, under 250 characters | On a generated or cached answer |
| `driver` | `string`, localized, under 210 characters | On a generated or cached answer |
| `actionable_recommendation` | `string`, localized, under 400 characters | On a generated or cached answer |

`scenario` is one of `RECENT_CONTENT_MOMENTUM`, `SEARCH_QUERY_SHIFTS`, `TRAFFIC_CHANNEL_SURGES`, `CATEGORY_RESONANCE`, `REFERRING_SITE_SHIFTS`, `STEADY_AND_SHIFTING`, `STEADY_BUT_DRIFTING`, `BASELINE_GROWTH`, `CURRENT_PERIOD_FORECAST`, `LOOKAHEAD` or `STEADY_STATE`. `DimensionType` is one of `TRAFFIC_CHANNELS`, `AUDIENCE_SEGMENTS`, `PAGES`, `SEARCH_QUERIES`, `DEVICES`, `CATEGORIES`, `REFERRING_SITES` or `HISTORICAL_BASELINE`.

`GET /v1/ai/operations/:operation_id` takes `site_id` and `site_secret` as **query parameters** and the bearer token as a header, and answers with the operation resource: `name`, `done`, `state`, `created_at`, `finished_at`, `response` carrying the generated fields on success, and `error` carrying a code and message on failure. It applies no rate limit, and it authorizes the caller as a registered user of the site rather than as the user who started the operation. An operation lives for one hour, refreshed when its worker finishes; past that a read is a `404`.

`site_id` and `site_secret` are injected by `Google_Proxy::request()` and are not part of the payload the data datapoint assembles. `hl` travels as a query parameter on the submit, not in the body. The plugin sends its own ranking: `ranked_dimensions` carries the ordering the datapoint already computed for the key factors section, translated into the service's `DimensionType` codes, and the service reasons from it rather than re-deriving one from `visitors` and `contextual_data`.

Failure responses to distinguish: `400` for a missing or invalid field, including a `baseline` with a non-positive `period_months`; `403` for an unverifiable token or a user not registered against the site; `404` for an operation that does not exist or has expired; `429` when the rate limit is exhausted, with no `Retry-After` header; and `500` for a parse or output-validation failure on the submit itself. **A failure inside an asynchronous generation is not a status on any response** — it marks the operation `FAILED`, which the operation read reports and the datapoint turns into a `WP_Error`.

### **Dimension code mapping** {#dimension-code-mapping}

The service's `DimensionType` codes against the factor catalog's own. The payload builder reads it left to right, translating the `dimensions` field's catalog codes into `ranked_dimensions`; [insight-driven ordering](#insight-driven-factor-ordering) reads it right to left, resolving `topDimensions` back onto the catalog:

| `DimensionType` | Catalog code |
| :---- | :---- |
| `TRAFFIC_CHANNELS` | `CHANNELS` |
| `DEVICES` | `DEVICES` |
| `AUDIENCE_SEGMENTS` | `VISITOR_MIX` |
| `REFERRING_SITES` | `REFERRERS` |
| `SEARCH_QUERIES` | `SEARCH_QUERIES` |
| `PAGES` | `CONTENT` |
| `CATEGORIES` | `CATEGORIES` |
| `HISTORICAL_BASELINE` | `HISTORICAL_BASELINE`, the entry this epic adds |

An unrecognized `DimensionType` maps to nothing and is dropped from the ordering, leaving the response's own ranking to place the rest.

[image1]: <data:image/png;base64,>

[image2]: <data:image/png;base64,>