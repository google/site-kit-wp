name: NPM Dependencies
description: Sets up Node.js, loads cache for NPM, installs dependencies, and runs a custom command.
inputs:
  command:
    description: A custom command to run.
    required: true
runs:
  using: composite
  steps:
    - run: nvm install
      shell: bash -l {0}
    - run: npm ci
      shell: bash
    - run: npm run ${{ inputs.command }}
      shell: bash
