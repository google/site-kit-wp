name: Playwright

on:
  push:
    branches:
      - develop
      - main
    # Ignore Storybook-only and unit-test-only changes when checking to run
    # E2E tests. This is useful when adding/modifying/refactoring/removing
    # test code ONLY in a commit, but saves running the expensive E2E test
    # suite on GitHub Actions when all that's changing are tests and not code
    # that would affect E2E behavior.
    paths:
      - ".github/workflows/playwright.yml"
      - ".babelrc"
      - "assets/**"
      - "includes/**"
      - "*.php"
      - "jest-puppeteer.config.js"
      - "composer.json"
      - "composer.lock"
      - "php-scoper/composer.lock"
      - "package.json"
      - "package-lock.json"
      - "postcss.config.js"
      - "webpack/*.config.js"
      - "webpack.config.js"
      - "tests/playwright/**"
      - "!assets/**/__tests__/**/*.js"
      - "!assets/**/test/*.js"
      - "!assets/**/*.test.js"
      - "!packages/**/__tests__/**/*.js"
      - "!packages/**/test/*.js"
      - "!packages/**/*.test.js"
  pull_request:
    branches:
      - develop
      - main
      - "feature/**"
    # Ignore Storybook-only and unit-test-only changes when checking to run
    # E2E tests. This is useful when adding/modifying/refactoring/removing
    # test code ONLY in a commit, but saves running the expensive E2E test
    # suite on GitHub Actions when all that's changing are tests and not code
    # that would affect E2E behavior.
    paths:
      - ".github/workflows/playwright.yml"
      - ".babelrc"
      - "assets/**"
      - "includes/**"
      - "*.php"
      - "jest-puppeteer.config.js"
      - "composer.json"
      - "composer.lock"
      - "php-scoper/composer.lock"
      - "package.json"
      - "package-lock.json"
      - "postcss.config.js"
      - "webpack/*.config.js"
      - "webpack.config.js"
      - "tests/playwright/**"
      - "!assets/**/__tests__/**/*.js"
      - "!assets/**/test/*.js"
      - "!assets/**/*.test.js"
      - "!packages/**/__tests__/**/*.js"
      - "!packages/**/test/*.js"
      - "!packages/**/*.test.js"
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

concurrency:
  group: playwright-${{ github.ref }}
  cancel-in-progress: true

env:
  GCS_BUCKET: site-kit-playwright
  FORBID_ONLY: true
  RETRIES: 2
  WORKERS: 4

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: github.event_name == 'push' || ( github.event_name == 'pull_request' && github.event.pull_request.draft == false )
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          tools: composer:2.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Get Composer Cache Directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@v5
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Composer Install
        run: composer install --no-interaction --no-progress --no-dev

      - name: Setup Node.js (.nvmrc)
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Prepare plugin
        run: |
          npm ci --workspaces --include-workspace-root
          npm run dev-zip
          npm run -w tests/playwright setup
          unzip -q google-site-kit.v*.zip -d plugin/

      - name: Upload plugin artifact
        uses: actions/upload-artifact@v6
        with:
          name: plugin
          path: plugin/google-site-kit

      - name: Upload Playwright browsers artifact
        uses: actions/upload-artifact@v6
        with:
          name: playwright-browsers
          path: ~/.cache/ms-playwright

  playwright-tests:
    name: WordPress ${{ matrix.wp_version }}
    needs: build
    strategy:
      fail-fast: false
      matrix:
        wp_version: ["latest", "nightly"]
        include:
          - wp_version: "5.2.21"
    env:
      WP_VERSION: ${{ matrix.wp_version }}
      PLUGIN_PATH: ${{ github.workspace }}/plugin/google-site-kit
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v6

      - name: Setup Node.js (.nvmrc)
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: npm install
        run: npm ci -w tests/playwright
        env:
          SKIP_PATCH_PACKAGE: '1'

      - name: Download plugin artifact
        uses: actions/download-artifact@v6
        with:
          name: plugin
          path: plugin/google-site-kit

      - name: Download Playwright browsers artifact
        uses: actions/download-artifact@v6
        with:
          name: playwright-browsers
          path: ~/.cache/ms-playwright

      - name: Restore Playwright browser permissions
        run: chmod -R +x ~/.cache/ms-playwright

      - name: Cache Docker images
        id: docker-cache
        if: matrix.wp_version != 'nightly'
        uses: actions/cache@v5
        with:
          path: /tmp/docker-images
          key: docker-${{ env.WP_VERSION }}-${{ hashFiles('tests/playwright/docker/wordpress/Dockerfile', 'tests/playwright/docker-compose.yml') }}

      - name: Load Docker images from cache
        if: matrix.wp_version != 'nightly' && steps.docker-cache.outputs.cache-hit == 'true'
        run: docker load -i /tmp/docker-images/images.tar

      - name: Build and cache Docker images
        if: matrix.wp_version == 'nightly' || steps.docker-cache.outputs.cache-hit != 'true'
        run: |
          echo "Building Docker images..."
          docker compose -f tests/playwright/docker-compose.yml build --quiet
          echo "Pulling images..."
          docker compose -f tests/playwright/docker-compose.yml pull --ignore-buildable --quiet
          if [ "${{ matrix.wp_version }}" != "nightly" ]; then
            echo "Saving Docker images to cache..."
            mkdir -p /tmp/docker-images
            docker save $(docker compose -f tests/playwright/docker-compose.yml config --images | tr '\n' ' ') \
              -o /tmp/docker-images/images.tar
          fi

      - name: Start environment
        run: npm run playwright:env:start

      - name: Run the tests
        run: npm run test:playwright

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: playwright-report-run[${{ github.run_attempt }}]-wp[${{ matrix.wp_version }}]
          path: tests/playwright/artifacts/

      - name: Set report target directory
        id: report-target
        if: always()
        run: |
          suffix="wp-${{ matrix.wp_version }}"
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "dir=pull/${{ github.event.number }}/${suffix}" >> "$GITHUB_OUTPUT"
          else
            echo "dir=${GITHUB_REF##*/}/${suffix}" >> "$GITHUB_OUTPUT"
          fi

      - uses: google-github-actions/auth@v3
        if: always()
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Upload report files
        uses: google-github-actions/upload-cloud-storage@v2
        if: always()
        with:
          path: tests/playwright/artifacts/playwright-html
          destination: ${{ env.GCS_BUCKET }}/${{ steps.report-target.outputs.dir }}
          parent: false
          process_gcloudignore: false

      - name: Upload no-cache report files
        uses: google-github-actions/upload-cloud-storage@v2
        if: always()
        with:
          path: tests/playwright/artifacts/playwright-html
          glob: "!(*.js*)"
          destination: ${{ env.GCS_BUCKET }}/${{ steps.report-target.outputs.dir }}
          parent: false
          process_gcloudignore: false
          headers: |-
            cache-control: no-store

  add-comment-to-pr:
    name: Add comment to PR
    runs-on: ubuntu-latest
    needs: playwright-tests
    if: always() && github.event_name == 'pull_request'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Find existing comment
        id: find-comment
        uses: peter-evans/find-comment@v3
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: github-actions[bot]
          body-includes: Playwright report is ready

      - name: Get comment body
        id: get-comment-body
        run: |
          body="Playwright report is ready:
          - [WordPress latest](https://storage.googleapis.com/${{ env.GCS_BUCKET }}/pull/${{ github.event.pull_request.number }}/wp-latest/index.html)
          - [WordPress nightly](https://storage.googleapis.com/${{ env.GCS_BUCKET }}/pull/${{ github.event.pull_request.number }}/wp-nightly/index.html)
          - [WordPress 5.2.21](https://storage.googleapis.com/${{ env.GCS_BUCKET }}/pull/${{ github.event.pull_request.number }}/wp-5.2.21/index.html)"
          delimiter="${body//$'\n'/'%0A'}"
          echo "body<<${delimiter}" >> "$GITHUB_OUTPUT"
          echo "$body" >> "$GITHUB_OUTPUT"
          echo "${delimiter}" >> "$GITHUB_OUTPUT"

      - name: Create or update comment on PR
        uses: peter-evans/create-or-update-comment@v4
        with:
          comment-id: ${{ steps.find-comment.outputs.comment-id }}
          issue-number: ${{ github.event.pull_request.number }}
          edit-mode: replace
          body: ${{ steps.get-comment-body.outputs.body }}
