import React from 'react';
import PropTypes from 'prop-types';

const SurveyQuestionOpenText = ( props ) => {
	return (
		<div>
			SurveyQuestionOpenText
		</div>
	);
};

SurveyQuestionOpenText.propTypes = {

};

export default SurveyQuestionOpenText;
