{
	"scripts": {
		"test": "playwright test --config=playwright.config.ts",
		"test:ui": "npm run test --silent -- --ui",
		"env:start": "docker compose up -d",
		"env:stop": "docker compose down"
	},
	"dependencies": {
		"@playwright/test": "^1.58.2",
		"mysql2": "^3.18.0"
	}
}